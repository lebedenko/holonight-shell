# U-02 Triage — Presentation Surfaces & Shell Composition Root

**Source**: docs/sdd/poc-readiness-review/units/U-02-surfaces.md (Investigation Targets section)
**Items in scope**: 10
**Methodology**: Static re-read of current HEAD at each cited file/location (line-by-line diff against the original citations, not trust of stale numbers); cross-checked every cited file against `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt`, running `git log -p <commit> -- <file>` on `d4f2310` (Phase 0) and `cf76ba0` (Phase 6) for the seven files that matched; one Qt API-contract question (`QQuickImageProvider::requestImage` threading guarantee, relevant to I-01) checked against official Qt 6 documentation rather than assumed from memory; no automated tool substituted for the code reading itself, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-01 | `TrayImageProvider` reads `TrayModel` row containers with no synchronization | Confirmed | 82/100 (re-derived) | Severity Medium · Effort M. Re-read `TrayModel.h` in full: no `QMutex`/`QReadWriteLock` member exists anywhere in the class (confirmed by inspecting all data members, `TrayModel.h:77-83`). `TrayImageProvider::requestImage()` (`TrayModel.cpp:364-381`) reads `rows_`/`index_by_key_` via `model_->imageForKey()` (`:236-247`) with zero locking, while `addItem`/`updateItem`/`removeItem` mutate the same containers on the main thread. **New observation beyond the original write-up**: checked Qt 6's official `QQuickImageProvider::requestImage` documentation (via Context7) — it states unconditionally "This method may be called by multiple threads, so ensure the implementation of this method is reentrant," a blanket contract requirement independent of whether any specific QML caller currently opts into async loading. Also traced the *only* production consumer of the `"tray"` provider end-to-end (`LayerShellManager.cpp:22` registers it; `TrayItem.qml` → `ExternalIcon.qml:64-78`'s plain `Image` element, which sets no `asynchronous` property, default `false`) — confirms the original's own hedge ("likely safe in practice") still holds today: no current call site triggers the off-thread path. Score reflects: clear, exhaustively-verified code citation (zero synchronization primitives exist, confirmed via full-file read) pushes the score up; the dormant/non-triggered-today status (no `asynchronous: true` anywhere in the tree) is the residual "subtle/latent" factor keeping it below a higher score. Code-change note: `TrayModel.h`/`TrayModel.cpp` were not touched in a way that affects this — see manifest tag below. [Phase 6: TrayModel.cpp — only `roleNames()` was changed (cached as `static const QHash`, see I-02); the `rows_`/`index_by_key_`/`imageForKey`/`TrayImageProvider::requestImage` code this item cites is untouched by that commit] still applicable in new form. |
| I-02 | `TrayModel::roleNames()` / `DbusMenuModel::roleNames()` rebuild the role hash on every call | Refuted | — | [Phase 6 commit `cf76ba0`, "fix: remediate phase 6 poc findings"] moot — already fixed, exactly as the original's own suggested direction proposed. `git log -p cf76ba0 -- libs/holonight-surfaces/src/TrayModel.cpp libs/holonight-surfaces/src/DbusMenuItem.cpp` shows both `TrayModel::roleNames()` (now `TrayModel.cpp:112-128`) and `DbusMenuModel::roleNames()` (now `DbusMenuItem.cpp:47-59`) rewritten from `return { ...literal... };` to a function-local `static const QHash<int, QByteArray> kRoles{ ...literal... }; return kRoles;`. Current-code read confirms the hash is now constructed exactly once per process (function-static init) and returned by reference on every subsequent call for both classes. The dataflow the original finding described (fresh `QHash` allocation on every invocation, repeated per new `DbusMenuModel` instance) no longer exists — original reasoning does not hold against current code. |
| I-03 | Clamped screen-edge-centering geometry duplicated between `TooltipSurface` and `StatusPopupGeometry` | Confirmed | 82/100 (re-derived) | Severity Low · Effort S. Direct side-by-side re-read: `TooltipSurface.cpp:108-111` still computes `centered_left`/`max_left`/clamps inline (`const int centered_left = anchor_x + (anchor_width / 2) - (kTooltipWidth / 2); ... std::clamp(centered_left, kScreenEdgeMargin, max_left)`), while `StatusPopupGeometry.cpp:39-61`'s dedicated, independently-unit-tested `statusPopupGeometry()` free function does the equivalent centering/clamping (`:45-48`) for a second, structurally near-identical case. The duplication itself is 100% unambiguous — a direct textual comparison, no timing/runtime dependency at all — which is why the score clears 80 despite Low severity (confidence tracks certainty the issue is real, not its impact). Corroborating analogy: this exact code *shape* (duplicated shadow/margin math) was independently confirmed by the original review as **[F-04]** at 85/100 and treated as a real, worth-fixing Confirmed Finding, not merely an Investigation Target — supporting that the underlying pattern class is genuinely actionable. [Phase 6: TooltipSurface.cpp and StatusPopupGeometry.cpp were both touched, but only to hoist the shared `kScreenEdgeMargin` constant into `ShellConstants.h` (this fixed a *different*, already-Confirmed finding, **[F-06]** triplicated-constant); the centering/clamping *formula* duplication I-03 flags is untouched by that refactor] partially addressed — the constant-duplication half of the surrounding mess was cleaned up in Phase 6, the formula-duplication half this item is actually about was not. |
| I-04 | Layer-shell surface classes never check `QQuickView` load status after `setSource()` | Confirmed | 85/100 (re-derived) | Severity Medium · Effort M. Personally re-grepped all seven cited files (`PerMonitorLayerManager.cpp:85`, `StatusPopupSurface.cpp:172`, `TooltipSurface.cpp:120`, `LauncherSurface.cpp:121`, `TrayMenuSurface.cpp:256,336`, `NotificationToastSurface.cpp:109`, `SidebarManager.cpp:238`) for `status()`, `QQuickView::Error`, or `statusChanged` — zero matches in any of them; every one calls `setSource(...)` and proceeds straight to `wl_surface_commit`/surface-visible state with no load-status gate. This is systemic and fully verified (not sampled) across the whole cited set, which is new relative to the original write-up (it asserted the pattern was "repeated" without an exhaustive per-file confirmation). Zero timing/runtime ambiguity — this is a static absence-of-a-check, confirmable with 100% certainty by reading each function. [Phase 0/Phase 6: three of the seven files were touched, but for unrelated reasons — `SidebarManager.cpp` gained `isKnownMonitor()` toggle validation in Phase 0 (see I-05/F-02 context); `TooltipSurface.cpp` and `TrayMenuSurface.cpp` had `kScreenEdgeMargin`/`kShadowPadding` constants centralized in Phase 6 — none of the three touched the `setSource()` call sites or added any status check] still applicable in new form. |
| I-05 | `StatusPopupSurface`/`SidebarManager`/`LauncherSurface` teardown hides the `QQuickView` before the layer-surface protocol object is destroyed | Not-a-bug | — | Re-read all three `destroySurface()` implementations. `StatusPopupSurface.cpp:179-201` now carries an explicit code comment absent from the original review's citation context: *"Hide immediately, but defer deletion: hide() can be invoked from a QML signal handler running inside one of these views (Esc handler, dismiss-overlay click). Deleting the QQuickView synchronously from there crashes ('object destroyed while a signal handler is in progress'), so tear down on the next event-loop pass via deleteLater()."* This is a documented, deliberate engineering trade-off, not an accidental ordering inversion. Two further points disprove the original's specific framing: (1) the "documented ordering invariant" the finding cites, `PerMonitorLayerManager.h:42-43` ("surface must be destroyed before view... member declaration order is load-bearing"), governs `MonitorSurface`'s `std::unique_ptr` destruction order for the *always-mapped* bar/wallpaper surfaces — a structurally different class hierarchy that `StatusPopupSurface`/`SidebarManager`/`LauncherSurface` do not inherit from or use; applying that invariant to these three by analogy over-reaches beyond what it actually documents. (2) CLAUDE.md's own "Layer-Shell Surface Hiding" gotcha — the actual project rule being invoked — warns specifically about *reusing* a hidden surface via a later `show()` (new `wl_surface` has no layer-shell role); all three classes here always construct a brand-new `QQuickView`/`LayerSurface` pair on next open (confirmed by re-reading `createSurface()`/`ensureSurface()` in each file) and never call `show()` on a previously-hidden view, so the exact failure mode the gotcha names cannot occur. `SidebarManager.cpp:245-261` and `LauncherSurface.cpp:127-140` use the identical hide-then-deferred-delete shape without their own comment, but given the shared origin and identical pattern across all three, the same crash-avoidance rationale is the more likely explanation than three independent accidental inversions. [Phase 0: SidebarManager.cpp gained `isKnownMonitor()` validation in `toggle()`; `destroySurface()` (the code this item examines) is untouched by that change] still applicable in new form — code shape unchanged, but re-reading it in full (plus the sibling comment) changes the verdict from "real deviation" to "deliberate, justified pattern." |
| I-06 | `decodePixmapList` decodes ARGB pixels one at a time via `QImage::setPixel`, and builds a debug string unconditionally | Confirmed | 84/100 (re-derived) | Severity Low · Effort S. [Phase 0 commit `d4f2310`, "fix: remediate phase 0 poc issues"] partially addressed — `git log -p d4f2310 -- libs/holonight-surfaces/src/TrayItem.cpp` shows Phase 0 fixed the adjacent, more severe **[F-01]** overflow/allocation-DoS finding by extracting `validateTrayPixmapDimensions()` and rejecting non-positive/oversized dimensions before any allocation — but that fix is orthogonal to I-06 and left both of I-06's specific complaints completely untouched in the current file: the unconditional debug-string build (`candidates.append(QStringLiteral("%1x%2/%3b")...)`, now at `TrayItem.cpp:66-68`, runs on every call regardless of log level) and the per-pixel `QImage::setPixel` loop (now `TrayItem.cpp:95-102`, unchanged logic, only re-indented/shifted by the F-01 refactor). Re-verified this is a real, unconditional cost on every tray-icon update for any 128×128+ icon (tens of thousands of bounds-checked `setPixel` calls). Clear, simple code citation, zero timing ambiguity — confidence reflects that plus the concrete current-line confirmation. |
| I-07 | `TooltipSurface` positions against `anchor_x` without subtracting the target screen's origin, unlike its two structural siblings | Confirmed | 86/100 (re-derived) | Severity Low · Effort S. This directly resolves the original write-up's own stated verification gap ("could not be fully verified from the C++ side alone... depends on which QML caller path is exercised"). Traced the QML call graph end-to-end: `TrayItem.qml:235` sets `anchorToPointerX: true` on its `BarTooltipArea`; `BarTooltipArea.qml:113-119`'s `showTooltipNow()` then computes `root.mapToGlobal(pointerX, 0)` — a genuine cross-monitor **global** desktop coordinate — and passes it straight through as `anchor_x` to `TooltipSurface.show()`. Current `TooltipSurface.cpp:108-111` clamps this value directly (`anchor_x + (anchor_width/2) - (kTooltipWidth/2)`) with no screen-origin subtraction, whereas its siblings do: `StatusPopupGeometry.cpp:45` (`local_anchor_x = anchor_x - screen_origin_x`) and `TrayMenuSurface.cpp:42` (`screen_local_request_x = screen_x - screen_geometry.x()`). This is a concretely production-exercised path (tray-item tooltips, the most frequently hovered topbar element), not a hypothetical — on any monitor with a nonzero geometry origin (i.e., any secondary monitor to the right/below the primary in a multi-monitor layout), the tooltip's horizontal clamp math operates on the wrong reference frame. [Phase 6: TooltipSurface.cpp was touched only to hoist `kScreenEdgeMargin` into `ShellConstants.h`; the screen-origin-subtraction gap this item flags is unchanged by that refactor] still applicable in new form. |
| I-08 | `ControlServer` has no message framing or size bound, and only processes the first `readyRead` chunk | Confirmed | 84/100 (re-derived) | Severity Medium · Effort S. `ControlServer.cpp` is not in the Phase 0-6 baseline manifest — untouched since the original review. Re-read `handleConnection()` (`ControlServer.cpp:49-58`): `connect(socket, &QLocalSocket::readyRead, this, [this, socket] { handleCommand(socket->readAll()); socket->disconnectFromServer(); });` confirms the exact pattern cited — single `readAll()` on first `readyRead`, immediate disconnect, no accumulation loop, no size cap, no timeout. Clear, unambiguous code citation with zero subtlety. Confidence reflects that the failure mode is real (a command split across two writes is truncated/misparsed; an oversized single write is read into an unbounded `QByteArray`) even though the attack surface is scoped to local processes with access to `$XDG_RUNTIME_DIR/holonight-shell/control.sock` (per CLAUDE.md's own documented control-socket usage). |
| I-09 | `WidgetManager::viewForMonitor` linear-scans the per-monitor surface map on every occupancy/workspace change | Confirmed | 81/100 (re-derived) | Severity Low · Effort S. `WidgetManager.cpp` is not in the Phase 0-6 baseline manifest. Re-read `viewForMonitor()` (now `WidgetManager.cpp:205-212`): `for (const auto& [screen, monitor_surface] : surfaces()) { if (screen->name() == monitor_name) ...}` confirmed unchanged, called from `applyVisibility` (now around `:113`) on every `onOccupancyChanged`. Simple, fully unambiguous code citation (an O(n) scan keyed by string comparison where a `QHash<QString, QScreen*>` would be O(1)) is why this clears the confidence bar despite Low severity — the original review's own text already correctly downgraded impact ("real-world cost is small given typical 1-4 monitor counts"), which this triage does not dispute; confidence here is about certainty the pattern exists, not about its performance significance. |
| I-10 | `TrayWatcher` uses `QStringList::contains()`/`removeOne()` (O(n)) for item-key membership checks on every property-fetch completion | Confirmed | 81/100 (re-derived) | Severity Low · Effort S. `TrayWatcher.cpp` is not in the Phase 0-6 baseline manifest. Re-read `registerTrayItemKey`/`unregisterTrayItemKey` (`TrayWatcher.cpp:138-166`) and every guard-check call site — confirmed at the *exact* line numbers the original cited (`:292,402,426,472`), with zero drift despite four other phases of changes elsewhere in the codebase, which independently corroborates the original citation was precise and this code path has had zero maintenance attention since. `registered_items_` remains a plain `QStringList`; every async D-Bus property-fetch completion lambda (IconName, IconPixmap, AttentionIconName/Pixmap, Title, ToolTip) linear-scans it via `.contains()`. Clear, simple citation, no timing ambiguity. |
