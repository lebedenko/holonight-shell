# U-11 Triage — Settings Application

**Source**: docs/sdd/poc-readiness-review/units/U-11-settings.md (Investigation Targets section)
**Items in scope**: 11
**Methodology**: Static re-read of all 11 cited C++/QML locations against current HEAD (re-located each citation, not trusted verbatim), cross-checked file-by-file against `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt`, with `git log -p <commit> -- <file>` run on every matching file (`SettingsEditModel.cpp`, `FontListModel.cpp`, `ThemeConfigFile.{h,cpp}`) to confirm whether the specific cited hunk — not just the file — was touched; external catalog behavior (`Holonight::defaultAccentId()`) verified by reading `/usr/include/holonight/theme_catalog.h` and its implementation in the sibling `holonight-qt` checkout; no automated tool substituted, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-C1 | Integer setters in `SettingsEditModel` have no range/positivity validation | Confirmed | 84/100 (re-derived) | [Phase 6: `SettingsEditModel.cpp` had its `setThemeModeSnapshot`/`markSaved(QString)` ternaries collapsed into `ThemeConfigFile::schemeForMode()` (commit cf76ba0) — that hunk sits above these setters and shifted their line numbers (was :135-178, now :131-178); the setters themselves are byte-identical to before — still applicable in new form.] Confirmed at current `SettingsEditModel.cpp:131-178`: `setUiFontSize`/`setFixedFontSize`/`setWorkspaceCount`/`setTrayMaxItems` all compare-and-assign with zero range/positivity checks. New observation beyond the original write-up: `libs/holonight-config/src/ConfigWriter.cpp:147-160` confirms the *write* path also does not clamp — it writes `count = <raw value>` and `max_items = <raw value>` verbatim, only appending an `# accepted: N-M` comment; clamping exists solely on the *read* side (`ConfigParsers.cpp:127` `clampRange()`, applied to `bar.workspaces.count`/`bar.systemtray.max_items` at parse time, not to `ui_font_size`/`fixed_font_size` at all). The post-Phase-6 recheck at `fe32997` confirms `ConfigParsers.cpp` changed only in time-to-event deadline parsing (`:330-373`); `clampRange()` and the relevant workspace/tray parse paths are unchanged. So an out-of-range value from a non-UI caller is durably persisted to `config.toml` in unclamped form, not merely rejected in memory. Score decomposition: clear code citation (both setter bodies and the writer's un-clamped emission, directly verified) pulls the score up; capped below 90 because the only realistic trigger remains a non-UI path (test, future direct binding) exactly as the original noted — masked entirely by QML `Slider` bounds today. |
| I-C2 | `setFromParsedConfig` bypasses the class's own guarded-recompute convention | Confirmed | 80/100 (re-derived) | [Phase 6: `SettingsEditModel.cpp` — the `schemeForMode()` extraction (commit cf76ba0) landed in `setThemeModeSnapshot`/`markSaved`, entirely below this function; `setFromParsedConfig` at lines 7-25 is untouched, byte-for-byte, since before the readiness review baseline — still applicable in new form.] Re-read at current `SettingsEditModel.cpp:7-25` confirms the exact original reasoning holds with no new information: `blockSignals(true)`/`blockSignals(false)` wraps two plain struct assignments (`current_ = config; snapshot_ = config;`), which emit no Qt signals on their own, making the block a no-op; all nine `*Changed()` signals then fire unconditionally regardless of whether any field actually differs; `is_dirty_ = false` is force-set directly instead of routing through `recomputeDirty()` like every other mutator in the class (`setThemeScheme`, `setUiFont`, etc., all confirmed at lines 88-174 to call `recomputeDirty()`). Confirmed at 80 — the safety net remains exactly what the original called out: `ConfigFileService::load()` (`ConfigFileService.cpp:63-64`) is still the sole caller, and it still immediately follows with `setThemeAppearanceSnapshot()` in the same call stack, so there is no live bug today, but the deviation from the class's established pattern is itself a present-tense, verifiable code-quality defect independent of that caller discipline holding forever. |
| I-C3 | `SettingsEditModel::markSaved(ParsedConfig, QString)` 2-arg overload is dead in production | Confirmed | 90/100 (re-derived) | [Phase 6: `SettingsEditModel.cpp:59-62` — this is the *exact* cited overload; commit cf76ba0 simplified its body from an inline ternary to `ThemeConfigFile::schemeForMode(theme_mode)` but did not touch its call graph — still applicable in new form.] Re-grepped the full repo for `markSaved(` call sites: `ConfigFileService.cpp:94` calls `model_->markSaved(config, theme_appearance)` — the 3-arg (`ThemeConfigFile::Appearance`) overload, confirmed at `SettingsEditModel.cpp:64-71`. The only other call site anywhere is `tests/test_settings_app.cpp:60` (`model.markSaved(model.toParsedConfig(), model.themeMode())`), which still targets the 2-arg overload. Declaration confirmed unchanged at `SettingsEditModel.h:54`. Score raised above the original 75 because this is now a fully grep-verified binary fact (zero production callers), not a probabilistic assessment — the only residual uncertainty is scope (repo-wide grep vs. exhaustive static-analysis guarantee), which caps it just short of the 90s ceiling reserved for citation-plus-external-verification items like I-C4. |
| I-C4 | Hardcoded `"cyan"` default-accent literal diverges from the catalog's actual default (`"default"`) | Confirmed | 92/100 (re-derived) | Not in the Phase 0-6 manifest (`SettingsEditModel.h` never appears there — only the paired `.cpp` does, and that diff never touches these initializers). Re-read at current `SettingsEditModel.h:77-78`: `current_theme_accent_{QStringLiteral("cyan")}` / `snapshot_theme_accent_{QStringLiteral("cyan")}`, unchanged. New verification beyond the original write-up: traced `ThemeConfigFile::defaultAccent()` (`ThemeConfigFile.cpp:26`) to `Holonight::defaultAccentId()`, and read its actual implementation in the sibling checkout (`/home/andrii/Projects/pet/holonight/holonight-qt/config/theme_catalog.cpp:139`): `QString defaultAccentId() { return QStringLiteral("default"); }` — confirms literally, not just per the original's paraphrase, that `"cyan"` is a *different, non-default* catalog option. Masking claim independently re-verified: `SettingsApplication.cpp:18` calls `file_service_->load()` before `view_->setSource(...)` at line 30, so QML never observes the constructor default. Score is high because both halves of the mismatch (the hardcoded literal and the catalog's real default) are now directly read from source, not inferred. |
| I-C5 | `FontListModel::rebuild()` re-scans the full system font list on every `fixedPitchOnly` toggle | Confirmed | 80/100 (re-derived) | [Phase 6: `FontListModel.cpp` — commit cf76ba0 only touched `roleNames()` (lines 49-52, converting the return to a `static const QHash`, which separately addresses D-C6); `rebuild()` at lines 16-30 is untouched — still applicable in new form.] Re-read at current `FontListModel.cpp:16-30` confirms `QFontDatabase::families()` is still called unconditionally on every `rebuild()` invocation (constructor + every `setFixedPitchOnly` toggle) with no cached `all_families_` member. New observation: the sibling anti-pattern in the same file (`roleNames()` rebuilding a `QHash` per call) was fixed by name in Phase 6 (D-C6), which shows this class does get remediation attention, yet this specific gap — cited as a separate, lower-confidence finding in the same original unit doc — was left alone, making "nobody has looked at this file" an unlikely explanation for it persisting. Score held at the 80 floor, matching the original write-up's own hedge ("likely low-impact since Qt caches its own internal font database") — no new evidence changes that assessment, so per the re-derivation guardrail this lands at the threshold rather than being inflated. |
| I-Q1 | `pragma ComponentBehavior: Bound` missing on files with delegates reaching outer-scope ids | Confirmed | 85/100 (re-derived) | Neither `NavPanel.qml` nor `AppearancePage.qml` is in the Phase 0-6 manifest (no QML files under `apps/settings/` appear there at all) — no code-change tag applies. `grep -n "pragma" apps/settings/qml/*.qml` returns zero matches across all 7 files, confirming the pragma is absent repo-wide in this unit, not just in the two cited files. Re-confirmed the outer-scope reach patterns: `NavPanel.qml`'s delegate `Text`/`Rectangle` children read `root.currentPage` (lines 43, 57, 65) from inside a `required property var modelData` delegate; `AppearancePage.qml`'s nested `Repeater` delegates read `editModel.themeScheme`/`editModel.themeAccent` (lines 89, 129) and the inner delegate reaches `familyDelegate.modelData.variants` (line 81) from outside its own scope. This is exactly the qmllint-blind-spot class the project's own CLAUDE.md documents at length for `ListView.section.delegate` under Bound mode, reinforcing that this codebase has hit adjacent instances of this exact gap before. Confidence held below 90 only because the practical failure mode (a future qmllint false-negative on a binding this pragma would have caught) is itself contingent on a future edit, not a present-tense runtime bug. |
| I-Q2 | Duplicate, independently-set "current page" state between `NavPanel` and `ContentStack` | Confirmed | 82/100 (re-derived) | Neither `NavPanel.qml` nor `ContentStack.qml` nor `SettingsWindow.qml` is in the Phase 0-6 manifest — no code-change tag applies. Re-read confirms exact duplication: `NavPanel.qml:11` declares `property string currentPage: "appearance"`, `ContentStack.qml:6` independently declares `property string currentPage: "appearance"` — two separate, unbound properties with matching literal defaults by coincidence, not by binding. The only synchronization path is `SettingsWindow.qml:31`: `onPageRequested: (key) => contentStack.showPage(key)`, fired solely from `NavPanel`'s own `MouseArea.onClicked` (`NavPanel.qml:73-76`). No `Binding`, no shared source of truth. Confirmed as a real structural gap: any second mutation entry point (keyboard shortcut, deep link, programmatic page switch) would desync the nav highlight from the displayed page with no compiler or runtime warning. Held at 82 rather than higher because — as the original noted — nothing observable is broken today; the single existing mutation path is closed and consistent. |
| I-Q3 | "Apply" and "Save & Apply" footer buttons are behaviorally identical and neither is gated on `isDirty` | Confirmed | 88/100 (re-derived) | `FooterBar.qml` is not in the Phase 0-6 manifest — no code-change tag applies. Re-read at current `FooterBar.qml:94-106` confirms the two `FooterButton` instances are functionally byte-identical: `enabled: !fileService.isSaving` / `onClicked: fileService.save()` on both, `accent: true` on both — the only difference is the label string. Directly contrasted with `FooterBar.qml:88-92`'s "Discard Changes" button, which correctly gates `enabled: editModel.isDirty && !fileService.isSaving`. This is fully verifiable from the QML text alone with no ambiguity, which is why the score sits higher than the original's 62: the original's confidence was capped by "confirm whether this is an intentional stub," but nothing in the file, `FooterBar.qml`'s surrounding code, or any project doc suggests intent — there is no comment, no TODO, no design-doc reference distinguishing the two buttons' purposes, so "unintentional duplication" is the only reading the current code supports. |
| I-Q4 | `HolonightTheme` singleton (once registered per D-C2) is used directly as a view data source, bypassing `editModel` | Not-a-bug | — | `AppearancePage.qml` is not in the Phase 0-6 manifest. Re-read confirms the citation (`AppearancePage.qml:58` `model: HolonightTheme.themeFamilies`, `:121` `model: HolonightTheme.accentOptionsForScheme(editModel.themeScheme)`) is accurate, but re-examining `SettingsEditModel`'s actual mandate refutes the premise that this *should* route through `editModel`. Per this unit's own Prior Context (item 2, independently confirmed by three agents in the original review): `SettingsEditModel`'s entire purpose is `current_`/`snapshot_` dirty-tracking of user-editable, persisted config fields — its `Q_PROPERTY` surface (`themeScheme`, `themeAccent`, `uiFont`, etc.) is exclusively the *currently-selected value*, never the *enumeration of available options*. The same file already establishes this exact convention for a second, unrelated catalog: `AppearancePage.qml:158`/`:173` binds `ComboBox { model: FontListModel { ... } }` directly, bypassing `editModel` for the identical reason — `FontListModel` is read-only reference data (all installed system fonts), not mutable settings state. `HolonightTheme.themeFamilies`/`accentOptionsForScheme(...)` are architecturally the same category of data (a static/read-only catalog enumeration) as `FontListModel`, not the same category as `editModel.themeScheme`/`themeAccent` (the mutable selections). Routing catalog enumeration through `editModel` would conflate two distinct concerns the class was deliberately kept narrow to separate. The "harder to unit-test in isolation" cost the original finding names is real but is the accepted tradeoff of that separation, not a defect. |
| I-Q5 | Repeated per-delegate `Qt.rgba` recomputation in `NavPanel` on every nav click | Confirmed | 84/100 (re-derived) | `NavPanel.qml` is not in the Phase 0-6 manifest. Re-read at current `NavPanel.qml:43-47` confirms both `Qt.rgba(...)` calls sit directly in the delegate's `color:` binding, re-evaluated for all 13 `Repeater` instances whenever `root.currentPage` changes (every nav click), even though each call is a pure function of `HoloniightPalette.accentViolet`/`HoloniightPalette.surface` and a fixed alpha (0.18 / 0.0) — never of `modelData`. This is fully statically verifiable: the palette tokens and alpha constants are compile-time-fixed inputs, so hoisting to a `readonly property color` on `root` is a mechanical, provably-equivalent transform. Confidence set above the original's 74 because the "pure function, no `modelData` dependency" claim — the crux of the finding — is directly confirmed by reading the binding expression itself, leaving no interpretive gap. |
| I-Q6 | `Text` elements across the app lack explicit `textFormat: Text.PlainText` | Confirmed | 80/100 (re-derived) | None of the 7 QML files are in the Phase 0-6 manifest. `grep -rn "textFormat" apps/settings/qml/` returns zero matches across the entire unit, confirming the finding's core claim repo-wide rather than only at the originally-sampled locations; a follow-up count of `Text {` declarations across the 7 files found 17 (close to the original's "~20" estimate, small deltas expected from exact-vs-approximate counting). All observed `text:` bindings in `AppearancePage.qml`/`BarPage.qml` (e.g. `editModel.uiFontSize + qsTr(" pt")`, `editModel.workspaceCount`) are plain interpolated literals with no markup, so `Text.AutoText`'s per-change markup-detection scan is pure overhead for every one of them. Held at the 80 floor rather than higher: this remains a micro-optimization (the original's own framing) whose real-world cost on ~17 short, infrequently-updated strings is plausible but not measured here — no new evidence shifts that assessment, so per the re-derivation guardrail the score stays at the Confirmed threshold rather than being inflated past it. |
