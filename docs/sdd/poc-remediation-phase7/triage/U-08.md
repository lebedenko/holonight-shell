# U-08 Triage — Topbar QML

**Source**: docs/sdd/poc-readiness-review/units/U-08-topbar.md (Investigation Targets section)
**Items in scope**: 8
**Methodology**: Re-read all 8 cited files at current HEAD (`apps/shell/qml/Topbar/WorkspacePillStrip.qml`, `BarSection.qml`, `WeatherSection.qml`+`WeatherWidget.qml`, `WorkspaceSection.qml`, `BatteryIndicator.qml`, `BarTooltipArea.qml`+`StatusPopupTriggerArea.qml`, `WorkspaceEdgeArrow.qml`, `ClockSection.qml`+`AudioWidget.qml`+`WeatherWidget.qml`), cross-referenced every cited file against `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt` and ran `git log -p cf76ba0 -- <file>` for each manifest hit; exhaustively grepped for corroborating/refuting evidence (all `BarSection { ... }` subclasses, all `BarTooltipArea { ... }` call sites, all `textFormat` occurrences, dead-property references) rather than trusting the original write-up's characterization — no automated tool substituted, matching REQ-NF-01.

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-01 | `WorkspacePillStrip.qml` unbounded `Repeater` sizing driven by `WorkspaceModel.maxWorkspaceId()` | Confirmed | 84/100 (re-derived) | Re-read at `WorkspacePillStrip.qml:21-23,36-51`: `stripCount: Math.max(root.windowStart + WorkspaceModel.displayCount - 1 + root.stripPad, WorkspaceModel.revision >= 0 ? WorkspaceModel.maxWorkspaceId() : 0)` feeds `Repeater { model: root.stripCount }` directly — unchanged from the original citation. New observation not in the original write-up: `Repeater` (unlike `ListView`) has **no delegate reuse/virtualization** — it eagerly instantiates one delegate per model index regardless of visibility, which is a documented, verifiable Qt Quick fact, not something requiring live reproduction. I independently re-read the `WorkspacePill.qml` delegate (not just asserted "nontrivial" as the original did) and confirmed it: a `Shape` with an 8-segment `ShapePath`, a `MultiEffect` glow, two `Behavior`/`SequentialAnimation` value sources, and a `required`-property chain — genuinely expensive per-instance. Combining "no virtualization" (new, decisive evidence) with the already-nontrivial delegate cost clears Confirmed. Severity Low-Medium, Effort Low (clamp `stripCount` to a sane cap, e.g. `Math.min(stripCount, windowStart + displayCount + 20)`, independent of `maxWorkspaceId()`'s raw value). *Code-change note: `WorkspacePillStrip.qml` does not appear in the Phase 0-6 baseline manifest — untouched since the original finding, still applicable in its original form.* |
| I-02 | `BarSection.qml` base `implicitWidth` formula is non-functional | Confirmed | 85/100 (re-derived) | Re-read at `BarSection.qml:8,20-27`: `implicitWidth: container.implicitWidth + 16` where `container` is a plain `Item` (not `Row`/`Column`/`Grid`/`Flow`) — plain `Item` never derives `implicitWidth` from children, so the expression is always `0 + 16`. This matches the original citation exactly (unchanged). New evidence beyond the original, which explicitly said it "could not confirm" reliance on the base formula: I grepped **all 11** current `BarSection { ... }` subclasses (`ActiveWindowSection`, `AudioWidget`, `BatteryWidget`, `ClockSection`, `KeyboardLayoutWidget`, `LogoSection`, `NetworkWidget`, `NotificationsWidget`, `StatusesSection`, `WeatherWidget`, `WorkspaceSection`) and confirmed **all 11 of 11** explicitly override `implicitWidth`. This closes the original's open question with a negative result across the entire current call-site population — the base formula is provably 100% dead/unreachable code today, not merely "masked." Severity Low, Effort Trivial (replace with `implicitWidth: 16` or drop the dead `+ container.implicitWidth`, or add a code comment marking it override-only). *Code-change note: `BarSection.qml` is not in the Phase 0-6 baseline manifest — untouched, still applicable in its original form. Note CLAUDE.md records a live-tested `WheelHandler` regression "on one topbar section" from a `MouseArea`→`WheelHandler` swap attempt; current `BarSection.qml:12-16` still uses the original wheel-only `MouseArea`, consistent with that swap having been reverted elsewhere — unrelated to this `implicitWidth` finding.* |
| I-03 | `WeatherSection.qml` `Loader` skips `WeatherWidget`'s shrink animation on `configured → false` | Confirmed | 83/100 (re-derived) | Re-read at `WeatherSection.qml:10-11`: `Loader { active: WeatherService.configured; visible: active }` — unchanged from the original citation. Re-read `WeatherWidget.qml:26-32`: the 200ms `Behavior on implicitWidth` is still present and still gated by the same `root.ready` condition. New grounding beyond the original (which only said "wasn't reproduced live"): `Loader.active: false` synchronously destroys the incubated item per documented Qt Quick `Loader` semantics (there is no asynchronous-destroy or animate-out hook — only `asynchronous` on *creation* applies) — this is a spec-level guarantee, not something that needs a live Hyprland session to establish, so it clears the Confirmed bar on static grounds alone. Severity Medium, Effort Low (gate `active` on an opacity/width threshold via `visible: opacity > 0` pattern per the codebase's own documented `visible`-vs-`Behavior` gotcha, or drive `active` from a delayed `Timer` after the shrink completes). *Code-change note: `WeatherSection.qml` is not in the manifest. `WeatherWidget.qml` (the child whose `Behavior on implicitWidth` this finding depends on) **is** in the manifest — `git log -p cf76ba0 -- WeatherWidget.qml` shows only dead-`id` removal (`frameCanvas`, `weatherIcon`, `textColumn`, `popupTrigger`, part of D-007's cleanup); the `Behavior on implicitWidth` block itself is untouched — still applicable in new form (same defect, same lines, minus four unrelated `id:` tokens).* |
| I-04 | `WorkspaceSection.qml:57` STA-2 — positioner `move:` transition, not a state-machine transition | Refuted | — | [Phase 6: `WorkspaceSection.qml` — removed unused `id: frameCanvas` from the `HudFrame` (D-007 dead-id cleanup); shifted the cited `move:` line from :57 to :56, no other change — still applicable in new form as far as line position goes, but re-derivation below refutes the underlying finding itself, not just moots the line number] Refuted. Re-read the **full current file** (133 lines, confirmed no truncation): there is no `states:` block anywhere in `WorkspaceSection.qml` — only the `Row { id: pillRow; move: Transition { NumberAnimation { properties: "x"; duration: 150 } } }` at line 56. The original finding's own risk premise is "if a future refactor adds named `States`... and someone copies this block into a top-level `transitions:` array, the missing `from`/`to` would make it fire for every state change" — per SPEC.md REQ-F-05's own example category ("the exact failure path cannot execute"), that failure path requires a `states:`/`transitions:` block that does not exist in this file today, and there is no SDD doc, TODO, or comment indicating one is planned. Separately: Qt Quick positioner `move:`/`add:`/`populate:` `Transition` properties are documented to interpolate each item's own old→new position automatically without `from`/`to` (this is the standard idiom, identical to `ListView.move`) — `from`/`to` are a `states:`-block-only concept, so the lint rule's premise doesn't even apply to positioner-level transitions. Both grounds independently disprove treating this as a live or even latent issue. |
| I-05 | `BatteryIndicator.qml` cosmetic snap when exiting the critical-pulse state | Confirmed | 81/100 (re-derived) | Re-read `BatteryIndicator.qml:73-99` in full (not previously line-cited by the original doc): the critical-pulse `SequentialAnimation { running: root.visible && root.critical; loops: Animation.Infinite; ... target: batteryIcon; property: "glyphScale" }` has no `alwaysRunToEnd`, and `states: State { name: "idle"; when: !root.critical; PropertyChanges { batteryIcon.glyphScale: 1.0 } }` has no matching `transitions:` entry. This is resolvable by static QML-semantics reasoning, contrary to the original's "not reproduced live" caveat: (1) an `Animation` with `running` driven false mid-cycle stops immediately at its current interpolated value (documented default, `alwaysRunToEnd` off) — so `glyphScale` is left at some value in `[1.0, 1.08]`, not necessarily `1.0`; (2) a `State` change with no `Transition` applies its `PropertyChanges` instantly. Composing these two documented, non-live-dependent behaviors reproduces the exact discontinuous jump the original finding describes — this is a states/animation-lifecycle defect class the codebase's own "visible: false beats a concurrent opacity Behavior" gotcha shows is routinely reasoned about statically, not a pointer/gesture/compositor-only class. Severity Low (purely cosmetic), Effort Low (add `Transition { NumberAnimation { property: "glyphScale"; duration: 150 } }` to the `states` block, or set `alwaysRunToEnd: true` on the pulse animation so it settles at 1.0/1.08 before the state cuts over). *Code-change note: `BatteryIndicator.qml` is not in the Phase 0-6 baseline manifest — untouched, still applicable in its original form.* |
| I-06 | `BarTooltipArea.qml`'s `barMonitorName` is not `required`, unlike `StatusPopupTriggerArea.qml`'s | Confirmed | 80/100 (re-derived) | Re-read `BarTooltipArea.qml:8`: `property string barMonitorName: ""` (still optional, silent empty-string fallback) vs. `StatusPopupTriggerArea.qml:11`: `required property string barMonitorName` (still required) — the asymmetry is unchanged. New verification beyond the original's "12 current call sites": I grepped **all** `BarTooltipArea { ... }` instantiations repo-wide (not just Topbar) — 13 current sites across `Topbar/` (11), plus `Tray/TrayItem.qml` and `Tray/TraySection.qml` — and confirmed **all 13** correctly set `barMonitorName`. I also checked whether `BarTooltipArea`'s broader (bar + tray) reuse might be an intentional reason for the looser contract (i.e., a `Not-a-bug` candidate) — it is not: both tray call sites route `barMonitorName` through identically to every bar call site, with no context where it is legitimately absent, so there is no architectural rationale for the asymmetry. Confidence lands at the 80 floor rather than higher: decomposition — clear code citation (two side-by-side property declarations, one call away from a bare grep) pushes it to the Confirmed bar, but the impact remains hypothetical (zero of 13 current sites actually omit it) which caps it exactly there rather than higher. Severity Low, Effort Trivial (`required property string barMonitorName` in `BarTooltipArea.qml`). *Code-change note: `BarTooltipArea.qml` is not in the manifest. `StatusPopupTriggerArea.qml` **is** — `git log -p cf76ba0` shows its `MouseArea.onClicked` swapped to `TapHandler.onTapped` (fixing D-004), but the `required property string barMonitorName` declaration at line 11 is untouched by that diff — still applicable in new form.* |
| I-07 | `WorkspaceEdgeArrow.qml` dead `hoverEnabled: true` on an unnamed `MouseArea` | Confirmed | 85/100 (re-derived) | Re-read `WorkspaceEdgeArrow.qml:64-70`: `MouseArea { objectName: "pointerArea"; hoverEnabled: true; enabled: root.active; onClicked: root.activated() }` — `hoverEnabled: true` still present, unchanged from the original citation. New evidence beyond the original (which only "could not confirm... vs. genuinely vestigial"): I grepped the **entire current file** for `containsMouse` — zero matches; the file's only hover-adjacent state (`_style.strokeColor`) is derived solely from `root.urgent`/`root.active`, both external props with no relation to this `MouseArea`. This is a definitive, static, whole-file negative-grep result (not a "could not confirm" hedge) — the property is unambiguously dead. This is squarely a static dead-code finding (unused boolean flag), not the pointer/gesture-*behavior*-swap class CLAUDE.md and the NMV guardrail reserve for live verification. Severity Low, Effort Trivial (delete `hoverEnabled: true`, or wire a hover-glow if one was actually planned — check `docs/sdd/workspace-indicator-redesign/` history first). *Code-change note: `WorkspaceEdgeArrow.qml` is in the Phase 0-6 manifest (26 lines changed) — `git log -p cf76ba0` shows Phase 6 moved the `MultiEffect { id: glowEffect ... }` block to declare *before* `Shape { id: chevron ... }` (fixing D-003's z-order violation); the `MouseArea`/`hoverEnabled` block at the end of the file is untouched by that diff — still applicable in new form, unaffected by the unrelated Phase 6 edit to this same file.* |
| I-08 | Missing `Text.PlainText` on frequently-updating labels | Refuted | — | [Phase 6: touches `ClockSection.qml` (dead-`id` + D-002 anchor/width-conflict fix — removed `right`/`rightMargin` from `clockColumn`'s anchors), `AudioWidget.qml` (dead-`id` removal), `WeatherWidget.qml` (dead-`id` removal) — none of these diffs touch `textFormat` or the `Text`/`Label` elements this finding cites; still applicable in new form as far as the underlying absence of `textFormat` goes] Refuted on severity grounds. Re-verified the technical premise is accurate: grepped every `.qml` file in `Topbar/` for `textFormat` — zero matches, confirming `ClockSection.qml`'s `timeLabel`/`dateLabel` (`Controls.Label`, updated every 1000ms via a `Timer`), `AudioWidget.qml`'s volume `Text`, and `WeatherWidget.qml`'s `tempText`/`condText` all still default to `Text.AutoText`. However, this re-read produced **no new evidence** beyond what the original write-up already had (it already stated "No file in the directory sets `textFormat` explicitly" — my grep just reconfirms the identical fact), and per the confidence-guardrail rule (DESIGN.md §5.2), reconfirming the same evidence without anything new caps the score near the original 62 — well below the 80 Confirmed floor, so this cannot be inflated into Confirmed. Applying the refutation the original review itself hinted at ("likely negligible... not a proven bottleneck"): `Text.AutoText`'s detection is a cheap linear scan for markup-indicating characters, not a full parse; for these specific short, fixed-format strings (`"HH:mm"`, `"NN%"`, `"NN°C"`) with no embedded markup, the scan is a fast early-exit on every call. Combined with the actual update cadence — 1Hz for the clock (nowhere near a per-frame hot path), and volume/temperature only on user/service-driven change events — the claimed performance concern does not hold at a severity that clears "real issue worth fixing." The original review's own hedge plus my independent no-new-evidence re-read together disprove the finding's implicit "worth a Confirmed fix" conclusion, per REQ-F-05's "exact failure path cannot execute [at a meaningful cost]" category. |
