# U-09 Triage — RightSidebar + Popups QML

**Source**: docs/sdd/poc-readiness-review/units/U-09-sidebar-popups.md (Investigation Targets section)
**Items in scope**: 7
**Methodology**: Static code review of all 7 cited locations against current HEAD (re-locating every line number rather than trusting the original citation); cross-checked each cited file against `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt`, and for every match ran `git log -p <specific-commit>` on that file to read the actual diff before judging moot/partially-addressed/still-applicable; one item (I-001) required reading the full body of `SessionIntegrationService::refresh()` and its Phase 3 rewrite to confirm the async dispatch mechanism; one item (I-005) required a manual height-budget arithmetic pass over `WeatherPopupContent.qml`'s fixed row heights against `StatusPopupGeometry.cpp`'s `kWeatherHeight` constant; no automated tool substituted, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-001 | Synchronous blocking diagnostics probe on every System-tab load | Refuted | — | [Phase 3: `libs/holonight-services/src/session-integration/SessionIntegrationService.cpp` — commit `4c4726a` "fix: remediate phase 3 poc blocking io" rewrote `SessionIntegrationService::refresh()` — **moot**, the finding's core claim no longer applies.] The original finding's exact mechanism (`collectDiagnostics()` calling all 7 `add*Diagnostics()` functions, including up to 7 blocking `QProcess::waitForStarted(1000)`/`waitForFinished(5000)` calls, synchronously inline on the calling thread) was removed in Phase 3. Current `refresh()` (lines 153–192) dispatches each of the same 7 diagnostic member functions via `QtConcurrent::run([this, diag_fn]() { return (this->*diag_fn)(); })`, combines the futures with `QtFuture::whenAll`, and only runs a `QFutureWatcher::finished` lambda on the main thread — which does cheap array concatenation (`diagnostics_.append(future.result())`), `deriveOverallStatus()`, and `emit diagnosticsChanged()`. The `QProcess` calls inside the diagnostic functions (verified: `QProcess proc; ... proc.waitForStarted(1000)... proc.waitForFinished(5000)` still present, e.g. around line 47–53) now execute inside the `QtConcurrent::run` worker-thread lambdas, not on the UI thread. `SidebarSystem.qml:51`'s `Component.onCompleted: SessionIntegrationService.refresh()` still fires on every System-tab (re)creation (line number unchanged, file not in manifest), but that call is now fire-and-forget and non-blocking (`refresh_in_progress_` guard prevents re-entrancy, per `POC Remediation Phase 3` project memory). The "measurable UI stall on the thread that also drives the topbar" claim is specifically what Phase 3 targeted and eliminated; no new blocking path was introduced in its place. |
| I-002 | Inconsistent `required property` usage across three delegates | Confirmed | 85/100 (re-derived) | [Phase 6: `apps/shell/qml/Popups/Network/WifiNetworkDelegate.qml` — commit `cf76ba0` "fix: remediate phase 6 poc findings" added a `trackedConnected`/`onTrackedConnectedChanged` repaint-trigger pair to the lock-icon `Canvas` (the D-004 fix) — **still applicable in new form**: the file's content changed, but not in a way that touches this finding.] Re-read the whole file: it still uses bare, non-required, non-`pragma`-bound identifiers throughout — `ssid`, `connected`, `strength`, `secured`, `known`, `index`/`row` are all referenced as implicit context-property roles in bindings (colors, text, `onPaint`, `onClicked`) with zero `required property` declarations and no `pragma ComponentBehavior: Bound` anywhere in the file (grepped for `pragma`: no match). `SidebarOverviewNotifications.qml:86-92,176` independently re-verified unchanged (file not in manifest): delegate root `Item { id: notifRow }` at line 86, `property var notif: modelData` (non-required) at line 92, bare `index` role read at line 176 — exact line numbers match the original citation. Score decomposition: high on the "clear code citation" axis (a `grep -c "required property"` returning 0 across the whole delegate file is unambiguous, zero interpretation risk) which pushes the score well past the 80 gate; not pushed higher because the original finding's own "not a live bug today" caveat still holds (all currently-read roles do exist on their backing models), so the residual risk is compile-time-safety-net loss, not a functional defect — a subtler, lower-severity axis that caps the score short of 90+. |
| I-003 | MultiEffect glow z-order inverted vs. the codebase's own documented convention | Confirmed | 83/100 (re-derived) | None of the three cited files (`Popups/Tray/TrayMenuPopup.qml`, `Popups/Tooltip/TooltipPopup.qml`, `RightSidebar/Tabs/Overview/SidebarOverviewCalendar.qml`) appear in the baseline manifest — no Phase 0–6 commit touched them; citation unchanged since the original review. Independently re-verified the "reference-correct" claim too, not just restated it: `Popups/Status/StatusPopup.qml` declares its `MultiEffect` (source: `panelShape`) at line 83, *before* the `Canvas { id: panelShape }` shape it glows at line 98, with an explicit comment at lines 77–78 ("Glow declared before the panel shape and labels so it renders behind them (MultiEffect z-order rule from CLAUDE.md)"). By contrast, current code confirms all three flagged files still declare `MultiEffect` *after* their source shape: `TrayMenuPopup.qml` has `Rectangle { id: panel }` at lines 86–96 then `MultiEffect { source: panel }` at lines 98–109; `TooltipPopup.qml` has `Rectangle { id: panel }` at lines 57–64 then `MultiEffect { source: panel }` at lines 68–79; `SidebarOverviewCalendar.qml` has `Rectangle { id: glowFrame }` at lines 198–207 then `MultiEffect { source: glowFrame }` at lines 209–218 — all three literally place the glow declaration after its source, which QML's default painter-order-by-declaration-order (CLAUDE.md's own documented "MultiEffect z-order" gotcha) renders on top of the source shape, not behind it. Score decomposition: declaration-order is a deterministic Qt Quick scene-graph fact (not compositor- or timing-dependent), so this is resolvable by static reading alone with no ambiguity — clear-citation axis pushes the score up; capped below high-80s/90s because the original finding itself notes "practical visual impact is likely small (opaque solid-color sources)," i.e., a real but low-severity consistency/maintainability defect rather than a visible bug. |
| I-004 | `BrightnessSlider` implements debounce, not the documented "throttle" | Confirmed | 88/100 (re-derived) | Not in baseline manifest — unchanged since original review; line numbers verified identical to citation. Re-read `RightSidebar/Tabs/QuickSettings/BrightnessSlider.qml` in full: line 9's header comment still reads "Throttles D-Bus writes to 100 ms during drag; writes immediately on release." The implementation at `onValueChanging` (lines 37–40) is `writeThrottle.pending = val; writeThrottle.restart();` — `Timer.restart()` resets the countdown on every fired event, which is the textbook debounce semantics (fires once, only after input activity pauses for the interval), not throttle semantics (fires periodically at a fixed cadence while input continues). During a continuous drag that never pauses for ≥100ms, `writeThrottle.onTriggered` never fires until `onValueCommitted` explicitly stops the timer and writes directly (lines 42–45) — so any second surface reading `BrightnessService.brightnessPercent` live would see no intermediate updates, only a single jump on release. Score decomposition: this is a pure semantic/terminology mismatch confirmable by reading `Timer.restart()`'s well-defined QML semantics against the stated intent in the same file's own doc comment — no external dependency, no timing race, no compositor state; the clearest of the 7 items on the "clear code citation" axis, hence the highest confidence score in this batch. |
| I-005 | `WeatherPopupContent.qml` has near-zero height margin with no overflow safety net | Confirmed | 81/100 (re-derived) | [Phase 6: `libs/holonight-surfaces/src/StatusPopupGeometry.cpp` — commit `cf76ba0` moved `kScreenEdgeMargin` out to `ShellConstants.h` (3-line diff) — **still applicable in new form**: `kWeatherHeight` remains `920` (line 16), unchanged by that refactor, so the height budget the finding's arithmetic depends on is unaffected.] `Popups/Weather/WeatherPopupContent.qml` itself is not in the manifest — untouched since the original review. Re-verified the structural claim directly: the file's root children are a plain `Column { id: stack; x: root.sidePad; y: root.topPad; ... }` (lines 36–41) with manual pixel heights on each `RowLayout` section (256, 148 via `WeatherHourlyStrip`, 210, 152, 20) and `spacing: 8`; grepped the whole file for `clip` and `Flickable` — zero matches, confirming no overflow containment exists. By contrast, all three sibling popups (`AudioPopupContent.qml`, `BatteryPopupContent.qml`, `NetworkPopupContent.qml`) use `ColumnLayout { anchors.fill: parent }` as their root layout — a self-constraining container, unlike Weather's manually-positioned `Column`. Independently summed the fixed section heights plus 7 inter-section gaps (`8*7=56`) plus `topPad` (8): the content stack totals roughly 900–910px against a `kWeatherHeight` content budget of exactly 920px (confirmed via `statusPopupContentSize()` in `StatusPopupGeometry.cpp` — the weather content area is allocated exactly 920px, no separate chrome subtraction inside that budget) — a slack in the same single-digit-to-low-double-digit-pixel range the original finding estimated, not a generous margin. Score decomposition: the structural gap (no `clip`, no `Flickable`, manual positioning vs. every sibling's safer `ColumnLayout`+`anchors.fill` pattern) is unambiguous from source; kept just above the Confirmed gate rather than higher because the exact overflow trigger point depends on unknown label/separator implicit heights I could only estimate, not read directly — a genuine "subtle reasoning" component per the scoring rubric. |
| I-006 | Notification-preview delegate lacks the discipline of its "rules" sibling | Confirmed | 82/100 (re-derived) | [Phase 5/6: `apps/shell/qml/RightSidebar/Tabs/Notifications/SidebarNotifications.qml` — commits `b204159` "fix: improve sidebar notification rule visibility" and `b082681` "fix: resolve notification application metadata" added `displayName`/`displayIcon` `required property` declarations plus an `ExternalIcon` row to the rules `Repeater` delegate — **still applicable in new form**: the reference file (the "disciplined" side of the comparison) became *more* disciplined, not less, while `SidebarOverviewNotifications.qml` (the file this finding says needs the fix) was not touched by any Phase 0–6 commit.] Re-verified both halves of the comparison independently rather than trusting the prior write-up: `SidebarNotifications.qml`'s per-app-rules `Repeater` delegate (lines 181–189) now declares six `required property` fields (`appName`, `displayName`, `displayIcon`, `ruleEnabled`, `urgencyFilter`, `index`) — even stronger discipline than what the original finding described. `SidebarOverviewNotifications.qml`'s live-preview `Repeater` delegate (lines 86–92, 176) is unchanged and remains undisciplined, as independently confirmed under I-002. The two "notification list" surfaces have therefore not converged — if anything the gap widened slightly since the original review. Score decomposition: the comparison itself is a direct file-vs-file read with no ambiguity (clear-citation axis, high), but this item is structurally a restatement/consequence of I-002 rather than an independently novel technical defect, which caps it in the low-80s rather than higher. |
| I-007 | `pragma ComponentBehavior: Bound` missing on several Overview/System delegates | Confirmed | 84/100 (re-derived) | [Phase 6: `apps/shell/qml/RightSidebar/SidebarTabBar.qml` — commit `b204159` "fix: improve sidebar notification rule visibility" added an unrelated `tabProfileGap` property and bottom-anchor lines to `tabColumn` (4-line diff) — **still applicable in new form**: the change shifted the `Repeater` block from its originally-cited line 57 down to line 61 in current HEAD, but did not touch the `Repeater`/delegate content itself or add the missing pragma.] Re-verified all four cited files individually at current line numbers rather than trusting old citations: `SidebarOverviewCalendar.qml` — `Repeater` at line 160 (`dayHeaderRow`, reads outer `dayHeaderRow.width`) and line 180 (`dayGrid`, delegate reads outer `dayGrid.width`) — exact match to citation, file not in manifest, unchanged. `SidebarOverviewNotifications.qml:86` — delegate `Item { id: notifRow }` reading outer `parent`/`iconContainer`/`timeLabel` ids — exact match, unchanged. `SidebarOverviewUpcoming.qml:342` — `delegate: Item { id: eventRow; ...}` at line 349 calls `root.getEventState(...)`, an outer-scope id reference — exact match, unchanged. `SidebarTabBar.qml` — `Repeater { model: root.tabMeta }` now at line 61 (shifted +4 from the pre-Phase-6 line 57) instantiates `SidebarTabButton` directly with `required property var modelData`/`required property int index` for the model roles, but the delegate also reads `root.currentTab` (line 71, an outer-scope id, not a model role) — the outer-id-without-pragma pattern this finding describes. Grepped all four files for `pragma`: zero matches in any; confirmed by contrast that `Popups/Tray/TrayMenuPopup.qml` and `Popups/Weather/WeatherHourlyStrip.qml` do declare `pragma ComponentBehavior: Bound` at line 1. Post-Phase-6 recheck at `fe32997`: `WeatherHourlyStrip.qml` adds a precipitation-icon row at lines 119–138, while its `pragma ComponentBehavior: Bound` remains at line 1; this supporting comparison still holds and the changed layout does not alter any cited delegate. Score decomposition: the pragma's absence and the outer-id references are both directly greppable/readable facts (clear-citation axis, high); not pushed above mid-80s because the practical performance cost (dynamic `QQmlContext` lookup vs. compiled binding) is real but small at this unit's actual repeater sizes (35 day cells, 6 tab buttons) — a low-severity, high-certainty finding. |
