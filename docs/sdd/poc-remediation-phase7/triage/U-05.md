# U-05 Triage — Desktop Integration, Appearance & Portal

**Source**: docs/sdd/poc-readiness-review/units/U-05-desktop-integration.md (Investigation Targets section)
**Items in scope**: 8
**Methodology**: Static re-read of all 8 cited locations against current HEAD (`libs/holonight-services/src/{portal,kde-compat,mime,session-integration}/`); every citation cross-checked against `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt`, and for any file present there, `git log -p <specific-phase-commit> -- <file>` was run directly (not inferred from the stat summary) to see the exact diff; no automated tool substituted, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-01 | `KdeCompatService` lacks the "completed" re-entrancy guard `MimeService` uses | Refuted | — | [Phase 5: `libs/holonight-services/src/kde-compat/KdeCompatService.cpp` (91 lines) and `.h` (46 lines) deleted in their entirety by commit `3fd9628` ("fix: remediate phase 5 poc findings") — moot]. Confirmed via `find`/`grep`: the files exist only as stale build artifacts (`build*/…/KdeCompatService.cpp.o`, clangd index) — zero source references remain anywhere under `apps/`, `libs/`, or `tests/` (`grep -rn "KdeCompatService"` returns nothing outside build dirs). The original finding's exact code (`proc` created in `runUpdateDesktopDatabase()`/`runKbuildsycoca()`, guarded only by `if (error != QProcess::Crashed)`) cannot exhibit a double-fire race because that code no longer exists to execute. Original reasoning does not "no longer hold" in the sense of being logically wrong at the time — it was correct — but it is inapplicable to current HEAD, which is the disposition Refuted captures under REQ-F-05's "the exact failure path cannot execute." |
| I-02 | `KdeCompatService`'s QML-facing API has no consumer anywhere | Refuted | — | [Phase 5: same commit `3fd9628` deleted `KdeCompatService.cpp`/`.h` outright — moot]. This is the direct resolution of the original finding's own "Suggested Direction" ("wire it in, or remove it if `SessionIntegrationService` has superseded its purpose") — the file was removed, not wired. `SessionIntegrationService` (confirmed still present, `apps/shell/qml/RightSidebar/Tabs/System/*` still references it) now covers the same diagnostic ground alone. No `kdeWarningActive`/`rebuildCaches`/`warningEmitted` symbols exist anywhere in current HEAD (`grep -rn` across `apps/`, `libs/`, `tests/` is empty), so there is no unwired API left to flag. |
| I-03 | `containsDesktopFiles`/`hasDesktopFiles` — identical helper duplicated across two files | Confirmed | 85/100 (re-derived) | [Phase 3: `SessionIntegrationService.cpp` underwent a major blocking-IO refactor in commit `4c4726a` ("fix: remediate phase 3 poc blocking io") that introduced `GuardedProcessRunner` and restructured most of the file (`+141/-100` per that commit's own diff), but the diff's hunk headers show `hasDesktopFiles()` itself was never inside a changed hunk — only its line number shifted, from the original `:123-126` to current `:126-129` — still applicable in new form. `ApplicationCacheRebuilder.cpp` (`containsDesktopFiles()`) is not in the Phase 0-6 manifest at all — untouched since before the readiness review.] Re-verified byte-for-byte at current locations: both functions are `QDirIterator iter(dir_path, {QStringLiteral("*.desktop")}, QDir::Files, QDirIterator::Subdirectories); return iter.hasNext();` — identical arguments, identical body, only the local variable/function name differs (`containsDesktopFiles` vs `hasDesktopFiles`). Confidence decomposition: clear code citation (exact literal match, not paraphrase) pushes the score up to 85; no timing/concurrency subtlety involved, so nothing pulls it down further — capped below 90 only because the fix (hoist to one free function) is genuinely low-stakes/low-blast-radius, not because there's doubt the duplication exists. |
| I-04 | `KdeCompatService::warningEmitted()` re-fires on every diagnostics recheck | Refuted | — | [Phase 5: commit `3fd9628` deleted `KdeCompatService.cpp`/`.h` in full — moot]. Same disposition as I-01/I-02: `recheckDiagnostics()` and `kde_warning_active_` no longer exist in any form in current HEAD, so the signal-re-emission asymmetry the original finding described cannot occur. |
| I-05 | "PortalBackend"/"PortalDBus" naming used for two unrelated, opposite-direction abstractions | Confirmed | 83/100 (re-derived) | Neither `portal/NullPortalBackend.h` (89 lines) nor `portal/SettingsPortalBackend.h` (72 lines) is in the Phase 0-6 manifest — both unchanged since the original review; no `[Phase N]` tag applies. Re-read in full: `NullPortalBackend.h` — a new observation absent from the original write-up — contains **no class literally named `NullPortalBackend`** at all; it defines `IPortalDBus` (abstract), `SystemPortalDBus` (real bus), and `NullPortalDBus` (test stub), none of which share the file's own name. `SettingsPortalBackend.h`, by contrast, does define a class matching its filename (`class SettingsPortalBackend`, the real `org.freedesktop.impl.portal.Settings` D-Bus service implementation). So the confusion is sharper than the original phrasing suggested: it isn't just "opposite-direction abstractions with adjacent naming," it's a file named `*Backend.h` that contains zero `*Backend` classes, sitting next to a file whose name-to-class mapping is exact — a reader skimming filenames alone has no signal to tell them apart. Confidence decomposition: high-certainty code citation (verified class list vs filename directly) pushes the score up; capped at 83 rather than 90+ because "naming clarity" retains an irreducible subjective-judgment component per DESIGN.md §5.2's guardrail against inflating clear-cut style findings into near-certainty scores. |
| I-06 | `MimeService` performs a full `QHash` copy-on-write detach on every single-key cache update | Confirmed | 80/100 (re-derived) | [Phase 3: `MimeService.cpp` was restructured in commit `4c4726a` (same phase-3 blocking-IO remediation, `+108/-166` net per its own diff, mostly consolidating the three `QProcess` helper call sites into `GuardedProcessRunner` calls) — the `old_cache`-snapshot-then-mutate pattern itself was not touched by that refactor, only relocated: originally cited at `:575-585`/`:596-615`, now at `:507-509` (`onQueryResult`) and `:531-533` (`setDefaultAsync`'s success callback) — still applicable in new form.] Re-verified the mechanism precisely (a new observation, not present in the original write-up): `const QHash<QString, QString> old_cache = mime_cache_;` is a cheap shallow ref-count bump under Qt's implicit sharing, but the very next line, `mime_cache_[mime_type] = desktop_file;`, calls `QHash::operator[]` on a still-shared container (refcount 2, since `old_cache` holds the other reference) — this forces exactly the full deep-copy detach the finding describes, on every single call, not just "sometimes." Confidence decomposition: the detach mechanism is now precisely nailed down (pushes up), but severity is genuinely low at current scale (≤~15 tracked mime roles per the original's own note, unchanged) and the original review itself flagged "not urgent" — this caps the score at the floor of the Confirmed range (80) rather than higher, since "real but minor" is a real but modest finding. |
| I-07 | `PortalService`'s async D-Bus probe calls rely on the ~25s libdbus default timeout | Confirmed | 88/100 (re-derived) | `portal/PortalService.cpp` is not in the Phase 0-6 manifest — no `[Phase N]` tag applies; file unchanged since the original review, cited line ranges still line up almost exactly (`nameHasOwner()` probe at :66, `introspectPortal()`/`listNames()` fan-out at :97/:100, `readSettingsAsync()`'s two `readSetting()` calls at :277/:282 — closely matching the original :59-70/:271-285/:199-217). New observation beyond the original write-up: `grep -rn "Timeout\|setTimeout"` across the entire `portal/` directory returns zero hits, and the concrete `SystemPortalDBus` implementation (`NullPortalBackend.cpp:21-41`) confirms every one of the four calls (`NameHasOwner`, `Introspect`, `ListNames`, `Settings.Read`) goes through a bare `QDBusInterface::asyncCall(...)` with no timeout argument — i.e., this isn't a case where a timeout exists but is merely uncited; there is provably no timeout anywhere in the call chain. This sits in direct, verifiable contrast to the unit's own established 5s convention (`GuardedProcessRunner`'s singleshot kill-timer, used by both `MimeService` and — post-Phase-3 — `SessionIntegrationService`). Confidence decomposition: maximally clear code citation (exhaustive grep across the whole directory, not a sampled read) plus a direct, still-valid contrast to a convention actively used elsewhere in this exact unit — the two factors DESIGN.md §5.2 calls out as score-raising — with no timing subtlety to pull it back down; capped just under 90 because the actual user-visible impact (stale `colorScheme`/`accentColor` for up to ~25s on an unresponsive broker) remains a rare edge case rather than a routine failure mode. |
| I-08 | Un-named magic numbers for freedesktop portal `color-scheme` enum values (1/2) | Confirmed | 81/100 (re-derived) | [Phase 1: `portal/SettingsPortalBackend.cpp` was touched by commit `1e6ff14` ("fix: remediate phase 1 poc issues"), but that change only deduplicated the file's local `themeConfigPath()` helper into the shared `ThemeConfigPath.{h,cpp}` (unrelated finding, now itself resolved) — it did not touch `colorSchemeForThemeConfig()`. The magic-number lines shifted from the original `.cpp:39-45` to current `.cpp:32-37` as a direct side effect of that same edit removing the ~9-line local `themeConfigPath()` function above it — still applicable in new form, line numbers moved only.] Re-verified at the new location: `colorSchemeForThemeConfig()` still returns bare `2 : 1` twice (lines 35 and 37) with no named constant, exactly as described. One correction to the original write-up, found on re-read: it characterized `PortalService::setColorScheme` (in the untouched, non-manifest `PortalService.cpp`) as an "inverted" second site of the same magic numbers — re-reading `PortalService::setColorScheme(int val)` (`PortalService.cpp:406-411`) shows it is a plain pass-through store (`color_scheme_ = val;`) with no `1`/`2` literal comparison anywhere in that function or its caller (`onSettingsReadReply`, which just does `static_cast<int>(result.toUInt())`) — that second site does not actually exist as described. This does not weaken the core `SettingsPortalBackend.cpp` finding, which is confirmed unchanged, but the fix scope is narrower than the original implied (one file, not two). Confidence decomposition: citation is exact and unambiguous (pushes up), but severity is style-only with a trivially small blast radius, so the score sits near the Confirmed floor rather than climbing further, and is further tempered by the correction to the original's secondary "inverted" claim. |
