# U-07 Triage — Calendar & Weather Services

**Source**: docs/sdd/poc-readiness-review/units/U-07-calendar-weather.md (Investigation Targets section)
**Items in scope**: 10
**Methodology**: Static re-read of all 10 cited locations against current HEAD (calendar/weather sources under `libs/holonight-services/src/{CalendarService.*,calendar/,weather/,weather-icon/}`); every citation whose file appears in `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt` was cross-checked with `git log -p <commit> -- <file>` against the specific Phase 0–6 commit that touched it (found: Phase 4 `2372ef6` and Phase 6 `cf76ba0` for this unit's calendar files); no automated tool substituted, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-01 | `WeatherIconMapper` `default:` attached to last switch case suppresses `-Wswitch` protection | Confirmed | 84/100 (re-derived) | `weather-icon/WeatherIconMapper.cpp` is not in the baseline manifest — unchanged by Phases 0–6. Re-read at current `dayLayers()` lines 180–183 and `nightLayers()` lines 236–239 (line numbers match the original citation exactly — no drift): both switches enumerate all 25 `Family` values and then attach `case Family::Unmapped: default: return {};` — a real enumerator's case doubles as the default, so a future `Family` addition falls through to `Unmapped`'s empty-layer behavior silently instead of tripping `-Wswitch`. New observation beyond the original write-up: `moonLayerName()` (lines 244–265) in the *same file* uses the safer pattern — an exhaustive switch with **no** `default:` label, falling back via a post-switch `return` — proving the safer idiom was already in use elsewhere in this file and simply wasn't applied consistently to the two `Family` switches. This sibling-pattern contrast is a concrete, current-code-only observation (decomposition: clear code citation, zero timing/subtlety — pushes score above the original 76). *Code-change note: location unaffected by Phases 0–6; original finding stands unchanged.* |
| I-02 | `CalendarSyncManager` `in_progress_`/`backoff_` maps keyed by account name only — cross-provider-type collision | Confirmed | 85/100 (re-derived) | [Phase 4: `CalendarSyncManager.h`/`.cpp` gained `removeAccount(provider_type, account_name)` (commit `2372ef6`, "remediate phase 4 poc findings") as part of wiring account-removal cleanup — still applicable in new form] Re-read confirms `in_progress_`/`backoff_` are still declared as plain `QHash<QString, ...>` keyed by `account` alone (`CalendarSyncManager.h:102-103`), and `runProviderSync()` still does `in_progress_.value(account, false)`/`backoff_.value(account)` with no provider-type discriminator (`.cpp:134-139`). New observation not in the original write-up: the Phase 4-added `removeAccount()` (`.cpp:206-211`) does `in_progress_.remove(account_name); backoff_.remove(account_name);` — also account-name-only — so a user with same-named CalDAV and ICS accounts now has a *second*, newly-introduced call site where removing one account's data can cross-contaminate the other's in-progress/backoff bookkeeping. This extends the original finding's blast radius with genuinely new evidence from code that didn't exist at the time of the original review, justifying a score above the original 70 (clear code citation at two sites, deterministic collision condition, no timing dependency). |
| I-03 | `WeatherProvider::parseReplyBody()` duplicates `checkComplete()`'s `check_reply` lambda | Confirmed | 82/100 (re-derived) | `weather/WeatherProvider.cpp` is not in the baseline manifest — unchanged by Phases 0–6. Re-read confirms `parseReplyBody()` (namespace-scope, lines 42–63) and the `check_reply` lambda inside `checkComplete()` (lines 174–197) both independently implement "read body → check `reply->error()` → check HTTP status == 200 → parse JSON", with near-identical structure but diverging error-message formats (`"network error: %1 (HTTP %2)"` vs `"network error on %1: %2 (HTTP %3)"`) — line ranges match the original citation exactly. *Code-change note: unchanged by Phases 0–6; original two-agent-corroborated reasoning holds with no new information required.* Decomposition: clear code citation, zero subtlety, deterministic — score set close to but above the original 68 given the exact-match re-verification. |
| I-04 | `CalendarCache::open()` failure silently disables calendar persistence with only a startup log line | Refuted | — | [Phase 4: `CalendarSyncManager::onSyncFinished` (commit `2372ef6`, "remediate phase 4 poc findings") now checks `!cache_.upsertEvents(*result) \|\| !cache_.reconcileAccountEvents(...)` and, on failure, routes through the same `handle_error()` path used for network errors, emitting `SyncError::Kind::StorageError` — moot] The original finding's specific claim — "no path from cache-open failure to `CalendarService::lastError`/`upcomingState` exists... this failure class is invisible" — no longer holds. Trace: if `cache_.open()` fails in the constructor, `open_` stays `false`, so every subsequent `CalendarCache::upsertEvents()`/`reconcileAccountEvents()` call returns `false` (`CalendarCache.cpp:358-360`); `onSyncFinished` now treats that combined failure as `SyncError{.kind = StorageError, .message = "Failed to persist calendar sync results"}` and calls `handle_error()`, which calls `emit syncError(...)` (`CalendarSyncManager.cpp:190-194`). `CalendarService::onSyncError` maps `StorageError` → `UpcomingState::ConnectError` and updates `lastError` (`CalendarService.cpp:172-192`). So the very first sync attempt after a failed cache-open now surfaces as a visible error state and message, not a silent "successful empty" state — this closes exactly the gap the original finding described, as a side effect of the Phase 4 account-removal-focused fix rather than a direct fix for this item. |
| I-05 | `CalendarEventModel::roleNames()` rebuilds a 29-entry `QHash` on every call | Refuted | — | [Phase 6: `CalendarEventModel.cpp` (commit `cf76ba0`, "remediate phase 6 poc findings") changed `roleNames()` from `return { ...29 entries... };` to a function-local `static const QHash<int, QByteArray> kRoles{ ...29 entries... }; return kRoles;` — moot] Re-read of current `CalendarEventModel.cpp:85-119` confirms the exact fix the original finding's own "Suggested Direction" asked for ("Cache as a function-local `static const QHash`") is already in place. The finding is fully resolved; nothing remains to confirm. |
| I-06 | `CalendarService::upcomingEvents()` is a `const` method returning a mutable `QAbstractListModel*` | Not-a-bug | — | [Phase 4: `CalendarService.cpp`/`.h` changed (commit `2372ef6`) for unrelated `onCalendarConfigChanged()`/`known_account_keys_` account-removal work — the cited `upcomingEvents()` accessor itself is untouched (still at `.cpp:31-36`, `.h:47`, byte-identical to pre-Phase-4) — still applicable in new form, i.e., unaffected by the phase's changes] This is the codebase's established, deliberate pattern for exposing a `QAbstractListModel*` through a `Q_PROPERTY READ` accessor: the accessor is `const` because it doesn't mutate the *owning* object's state (it just returns an existing pointer), while the returned pointer is intentionally non-const because QML `ListView`/`Repeater` bindings and any C++ consumer need a mutable model handle to call mutating methods on it. Architectural precedent confirmed via `grep` across `libs/holonight-services/src/`: `AudioService::outputs()/inputs()/playbackStreams()/recordingStreams()` (`audio/AudioService.h:48-51`) and `NetworkService::wifiNetworks()` (`NetworkService.h:93`) all use the identical `const` + mutable-model-pointer shape. The original review itself self-caveated this as "flagged only because it matches the review's literal criterion... likely not worth changing," which this re-verification confirms with a concrete cross-file precedent the original write-up didn't cite. |
| I-07 | `WeatherService` retries indefinitely on an invalid/revoked API key with no distinct "authentication failed" signal | Confirmed | 82/100 (re-derived) | `weather/WeatherService.cpp` is not in the baseline manifest — unchanged by Phases 0–6. Re-read confirms `onFetchError()` (lines 362-371) and `nextBackoffMs()` (lines 391-401) — matching the original citation exactly — treat every fetch failure identically: `WeatherProvider::checkComplete()`'s `check_reply` lambda formats a 401/403 the same as any other non-200 status into a free-form `error_msg` string (`"HTTP %1 on %2"`, `WeatherProvider.cpp:184-186`) with no structured status code preserved by the time it reaches `WeatherService::onFetchError(const QString& message)`. New observation beyond the original write-up: because the error signal's payload is already reduced to a display string before `WeatherService` ever sees it, a fix can't simply pattern-match "401" server-side in `WeatherService` without either parsing that string (fragile) or plumbing a structured status/kind through `WeatherProvider::fetchError` — a materially more specific implementation constraint than the original "special-case HTTP 401/403" suggestion implied. Decomposition: clear code citation across both files, deterministic reproduction (any revoked key), no timing dependency — score set above the original 65. |
| I-08 | `WeatherService::hourlyVariant()`/`dailyVariant()` rebuild a `QVariantList` on every property read | Confirmed | 80/100 (re-derived) | `weather/WeatherService.cpp` is not in the baseline manifest — unchanged by Phases 0–6. Re-read confirms `hourlyVariant()`/`dailyVariant()` (lines 473-489, matching the original citation exactly) still allocate and populate a fresh `QVariantList` from `hourly_`/`daily_` on every call, with no cached/invalidated-on-`forecastChanged` copy. `currentVariant()` (line 471) uses the same "convert on every read" shape for a single value, confirming the pattern is systemic to this accessor group, not isolated to the two flagged methods. Decomposition: clear code citation, deterministic gap — capped just at the 80 threshold rather than higher because the original's own caveat still holds unchanged: cost is bounded to ~once per 10-minute fetch for binding-driven QML access, and only imperative/`Repeater`-style re-reads would actually hit this on every access, which is a real but usage-pattern-dependent (not universally reproducible) cost. |
| I-09 | `ICalParser::accumulateVEventProp` does up to ~26 sequential string comparisons per RFC 5545 property line | Confirmed | 80/100 (re-derived) | `calendar/ICalParser.cpp` is not in the baseline manifest — unchanged by Phases 0–6. Re-read of `accumulateVEventProp()` (lines 277-351, matching the original citation exactly) confirms an `if`/`else if` chain dispatching on `prop.name` — counted 28 distinct property-name branches (`UID` through `RESOURCES`) in current code, consistent with the original's "~26" estimate. Decomposition: clear, exhaustively-verifiable code citation, zero subtlety — but score is capped at 80 (not pushed higher) because the original's own severity-capping rationale is re-confirmed unchanged on re-read: this runs on a background `QtConcurrent::run` thread at most every 15/60 minutes per account, so it is not UI-blocking, matching the original review's explicit "capped severity accordingly" framing. |
| I-10 | No range/sanity validation on weather numeric fields from the API — malformed responses propagate nonsensical values to the UI | Confirmed | 81/100 (re-derived) | `weather/WeatherProvider.cpp` and `weather/WeatherData.h` are not in the baseline manifest — unchanged by Phases 0–6. Re-read confirms `parseCurrentJson()` (`WeatherProvider.cpp:250-282`, matching the original citation) reads `humidity`/`clouds` via `.toInt(0)` and `populatePollutionData()` reads `aqi` via `.toInt(0)` (line 96) with no `std::clamp` or range check anywhere in the parse path; `WeatherData.h`'s `Q_GADGET` structs (`HourlyEntry`/`DailyEntry`/`CurrentWeather`) likewise carry only default member initializers, no invariant validation on construction. New observation beyond the original write-up: by contrast, `condition_id`/OWM condition codes *do* have a bounded fallback — `WeatherIconMapper::conditionFamily()` and `WeatherService::iconTable()` both safely degrade unknown codes to `Family::Unmapped`/`wsymbol_0999_unknown.svg` — so the codebase already has a precedent for "validate and fall back safely" that the numeric weather fields (humidity, clouds, pop, aqi) conspicuously lack, sharpening the original finding's severity assessment slightly. Decomposition: clear code citation across two files, deterministic (any malformed/out-of-spec API response), no timing dependency — score set just above the original 62. |
