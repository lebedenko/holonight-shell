# U-03 Triage — Power, Idle & Brightness Management

**Source**: docs/sdd/poc-readiness-review/units/U-03-power-idle-brightness.md (Investigation Targets section)
**Items in scope**: 10
**Methodology**: Static re-read of all cited locations against current HEAD (each item's file(s) re-opened and re-line-numbered independently of the source doc's citations); every citation cross-checked against `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt`, with `git log -p <specific-phase-commit> -- <file>` run for every matching file (`ShellApplication.cpp` for I-02, `IdleService.cpp`/`SysfsBackend.cpp` for I-03/I-05/I-10) to see the exact diff rather than inferring from file-level manifest presence alone; no automated tool substituted, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-01 | `BatteryService::setPercent()` / `LowBatteryMonitor` accept out-of-range UPower percentages unclamped | Confirmed | 83/100 (re-derived) | Re-read confirms `BatteryService::setPercent()` (`BatteryService.cpp:131-137`) and `LowBatteryMonitor::checkThresholds()` (`LowBatteryMonitor.cpp:72-91`) still apply no `std::clamp(0,100)` anywhere on the UPower-sourced percent — both files are absent from the Phase 0-6 manifest, so the finding is unaddressed, unchanged. New observation beyond the original: traced the concrete failure path for a negative percent — `checkThresholds()`'s `pct <= critical_threshold_` (default 10) means any negative UPower reading immediately satisfies the critical branch and fires a D-Bus `Notify` call with the literal broken string `"-N% remaining — plug in now"` (`LowBatteryMonitor.cpp:82-83`), a demonstrable user-visible corruption path, not just an internal state concern. Decomposition: pushed up by this concrete reachable failure-string demonstration (clear code citation) and zero staleness risk (neither file touched by Phases 0-6); held below 90 because the trigger (out-of-range ACPI/UPower firmware report) is a hardware-dependent edge case, not exercised in normal operation. Code-change note: neither file appears in the manifest — original finding fully unaddressed. |
| I-02 | `ActivityGateManager` holds raw non-owning pointers with no unregister path; Qt child-deletion order creates a dangling-pointer window on shutdown | Refuted | — | `[Phase 6: cf76ba0 — ShellApplication.cpp/.h: activity_gate_manager_ moved into the member-init list as the first-constructed (thus first-destroyed) member, exactly matching this finding's own suggested direction — moot]`. Re-reading `ShellApplication.cpp` (in the manifest) shows Phase 6 directly fixed this ordering hazard: `activity_gate_manager_` was moved from a body assignment (previously constructed *after* `calendar_service_`/`weather_`/`suspend_inhibitor_service_`, matching the original finding's premise) into the constructor's member-initializer list as the *first* initialized member (`ShellApplication.cpp:95`, now preceding `calendar_service_` at `:97`, `weather_` at `:112`, `suspend_inhibitor_service_` at `:131`). `ShellApplication.h` now carries an explicit new comment: "Constructed first (destroyed first) so it never outlives an IActivityGate implementer it holds a raw, non-owning pointer to." Qt destroys direct `QObject` children in the order they were added to the parent's children list (construction order) — `activity_gate_manager_`, now first in that list, is destroyed *first*, before any of the three gates it references; its destructor is defaulted and touches nothing, and once its own `~QObject` begins, Qt disconnects `lid_monitor_`'s `lidStateChanged` connection to its `onLidStateChanged` slot, so no dangling-pointer call into `gates_` is reachable during teardown. Verified via `git show cf76ba0 -- apps/shell/app/ShellApplication.cpp apps/shell/app/ShellApplication.h`. |
| I-03 | `SuspendInhibitorService`/`SysfsBackend`/`IdleService` each hardcode their own `2000`ms subprocess timeout as a bare literal | Confirmed | 82/100 (re-derived) | `[Phase 1: 1e6ff14 — IdleService.cpp/SysfsBackend.cpp's copies of this pattern were removed via the F-01 shared-helper extraction (LogindSessionResolver.cpp), but the extracted helper still uses a bare 2000 literal and SuspendInhibitorService.cpp (never touched by Phases 0-6) retains both original sites unchanged — partially addressed]`. Re-reading confirms `SuspendInhibitorService.cpp:60` (`listInhibitors`' `busctl.waitForFinished(2000)`) and `:119` (`startAsyncPoll`'s `timeout->setInterval(2000)`) are byte-for-byte unchanged, still sitting a few lines below the file's own `kPollIntervalMs{5000}` (line 14) — zero drift, file absent from the manifest. New finding beyond the original: `IdleService.cpp:191`/`SysfsBackend.cpp:102`'s copies are *gone* — Phase 1 extracted the duplicated loginctl-session-resolution logic (the [F-01] Confirmed Finding's fix) into `LogindSessionResolver.cpp`, collapsing 2 of the original 4 magic-number sites into one shared call — but that extraction carried the bare `2000` forward unnamed (`LogindSessionResolver.cpp:56`) instead of introducing a constant, so the inconsistency survived in reduced form (3 sites now vs. 4). Further new observation: a reusable `runGuardedProcess(program, args, timeout_ms, callback)` helper (`GuardedProcessRunner.cpp/h`, added Phase 3) already exists and is used by `MimeService.cpp` with a caller-supplied timeout, but `SuspendInhibitorService::startAsyncPoll` (lines 104-144) still hand-rolls its own `QProcess`+`QTimer`+kill guard instead of adopting it — the fix pattern exists off-the-shelf and simply wasn't applied here. Post-Phase-6 recheck at `fe32997`: `GuardedProcessRunner.cpp` now moves `GuardedProcessResult` into its callback, an unrelated ownership/performance cleanup; its timeout API and availability as a replacement remain unchanged. Decomposition: pushed up by the git-verified partial fix plus discovery of an existing, unused remedy; held to 82 because severity remains Low/cosmetic (no functional risk). |
| I-04 | Dead/duplicate branch in `PowerProfilesService::parseProfileNames` | Confirmed | 83/100 (re-derived) | `PowerProfilesService.cpp` is absent from the manifest; `parseProfileNames` is still at the originally-cited lines 163-211, unchanged. New observation beyond the original's "appears unreachable in practice" hedge: traced Qt's `QVariant::canConvert` semantics precisely — `canConvert<QDBusArgument>()` at line 201 can only return true if the variant's registered type has a conversion path *to* `QDBusArgument`; no such converter exists for any type other than `QDBusArgument` itself (it is a wire-serialization iterator, not a value type third parties register converters into), so once execution reaches line 201 having already failed the exact-type check `value.userType() == qMetaTypeId<QDBusArgument>()` at line 173, `!value.canConvert<QDBusArgument>()` is unconditionally true — lines 204-210 are provably unreachable, not merely unlikely. This also means the CLAUDE.md-documented anti-pattern (a bare `canConvert<QDBusArgument>()` probe risking a "write from a read-only object" warning) sits in genuinely dead code here, not live code — a nuance the original write-up didn't draw out. Decomposition: pushed up by the definitive (not probabilistic) unreachability proof via Qt API semantics — a clear code citation the original only gestured at; held below 90 because this is Low-severity/Effort-S dead-code cleanup, not a functional defect. Code-change note: file not in the manifest — unaddressed. |
| I-05 | `SysfsBackend` destructor closes the inotify fd before disabling the `QSocketNotifier`, contradicting its own header comment | Refuted | — | `[Phase 1: 1e6ff14 — SysfsBackend.cpp's resolveSessionPath()/includes were rewritten (unrelated F-01 dedup), shifting downstream line numbers — destructor cited originally at :41-48 is now :37-44, header comment unchanged at :44-46 — still applicable in new form only as far as citation line numbers moved; substantive code is byte-for-byte identical to pre-Phase-1]`. The factual code/comment mismatch is real and reproduced exactly in current code — `SysfsBackend.h:44-46`'s comment ("notifier_ must be declared after ifd_/iwd_ so its destructor fires first") misstates C++ semantics (an explicit destructor *body* always finishes before any member's destructor runs, regardless of declaration order), and `~SysfsBackend()` (`.cpp:37-44`) does call `inotify_rm_watch`/`close(ifd_)` before `notifier_` is torn down. However, re-examination shows the *safety property* the finding actually worries about (a dangling-fd corruption window) does not exist here: this unit's own review already independently verified "thread safety is clean — zero cross-thread constructs across all 33 files" (U-03 source doc's Tool Sign-off), and within a single-threaded, non-reentrant Qt destructor call, no code (event-loop dispatch, another handler) can execute between the destructor body's `close(ifd_)` and the immediately-following synchronous destruction of the `notifier_` member — there is no yield point for the closed fd number to be reused by a new resource and misdelivered to the about-to-be-destroyed notifier. The comment's stated *mechanism* is wrong, but the code's actual behavior is safe by construction of this project's single-threaded execution model, not by declaration order. This refutes the finding's implicit safety concern even though its comment-accuracy observation is technically correct; a comment fix remains worthwhile but is not evidence of an exploitable defect. |
| I-06 | `BrightnessService::computePercent` has no clamp on raw brightness outside `[0, max_brightness_]`, asymmetric with the write-path clamp | Confirmed | 84/100 (re-derived) | `BrightnessService.cpp` is absent from the manifest; `computePercent` (lines 43-48, unchanged) still has no `std::clamp`, while `setBrightnessPercent` (write path, line 54) does. New observation beyond the original's abstract "a driver reporting brightness slightly above max_brightness... could emit an out-of-range percent": traced the concrete call graph — `computePercent` is invoked from `onBrightnessChanged` (line 60), driven exclusively by `SysfsBackend::onInotifyEvent()`'s inotify-triggered re-read of the raw sysfs `brightness` file — a file writable by *any* process with sufficient permission (root, or a udev-granted video-group ACL), entirely outside this shell's control. This ties directly into this same unit's own already-Confirmed **[F-05]** finding (SysfsBackend has no device-removal detection) and this triage's own **I-10** (no `QTextStream` fail-state check in `readBrightness`) — both establish that raw sysfs values reaching `computePercent` are an already-known-unreliable input in *this exact codebase*, not a generic hypothetical. Decomposition: pushed up by this concrete, externally-triggerable reachability path plus the cross-reference to the unit's own already-Confirmed F-05 threat model; held below 90 because impact is a cosmetic UI value (slider/percent display), not a crash or data-loss path. Code-change note: file not in the manifest — unaddressed. |
| I-07 | `PowerProfilesService` never disconnects its `PropertiesChanged` D-Bus subscription on daemon restart — signal bindings accumulate | Confirmed | 82/100 (re-derived) | `PowerProfilesService.cpp` is absent from the manifest; `initFromService()` (lines 65-99, unchanged) calls `dbus_->connectSignal(...)` unconditionally on every `serviceRegistered` event (wired at line 56's unconditional lambda), and `clearState()` (lines 154-161, unchanged) never pairs this with a `disconnectSignal` call. New observation beyond the original's "`QDBusConnection::connect()` doesn't deduplicate identical registrations" (stated as general Qt knowledge): traced the actual wrapper implementation used here — `QtDbusPropertyClient::connectSignal` (`libs/holonight-platform/src/DbusPropertyClient.cpp`) forwards directly to the 6-argument `QDBusConnection::connect()` overload with no `Qt::UniqueConnection` flag and no internal already-connected tracking, removing residual doubt that some intermediate dedup layer exists — each daemon restart during the process's uptime genuinely adds one more live binding. Decomposition: pushed up by this concrete, code-verified confirmation that no dedup exists at any layer; held below 90 because `onPropertiesChanged`'s own equality-gated setters (`applyActiveProfile`/`applyProfiles`) make the practical symptom redundant re-processing, not corruption — matching the original's own "redundant work, not visible corruption" framing, which still holds. Code-change note: file not in the manifest — unaddressed. |
| I-08 | `BrightnessService::setBrightnessPercent` silently no-ops with no log when `max_brightness_ == 0` | Refuted | — | The code shape is accurate — the `if (max_brightness_ == 0) { return; }` guard at lines 51-53 logs nothing. However, tracing every call site (grep across `apps/shell/qml/` and `libs/`) shows `setBrightnessPercent` has exactly **one** caller in the entire codebase: `BrightnessSlider.qml`, whose root item is `visible: BrightnessService.hasBacklight` (`BrightnessSlider.qml:14`) — the slider is not rendered/interactive when `hasBacklight` is false. `hasBacklight()` is `max_brightness_ > 0` (`BrightnessService.h:35`), and `max_brightness_` is assigned exactly once, in `init()` at construction, never reassigned anywhere afterward (verified — no other write site in `BrightnessService.cpp`). Structurally: whenever `setBrightnessPercent` is reachable (slider visible, `hasBacklight` true), `max_brightness_` is guaranteed nonzero for the object's entire lifetime, so this guard can never trigger through the only path that invokes the method; whenever `max_brightness_` *is* zero, the slider is hidden and the method is never called (no D-Bus/control-socket/other `Q_INVOKABLE` caller exists — `BrightnessService` is referenced only from `ShellApplication.cpp/.h` registration and this one QML file). The "brightness-key press was dropped, hard to tell from logs" scenario the finding worries about has no corresponding code path — physical brightness keys are not wired through this `Q_INVOKABLE` in this codebase. This refutes the finding's practical premise even though the missing-log observation is literally true; the guard is presently unreachable dead code, closer in nature to **I-04**'s dead-branch finding than to a real silent-failure gap. File absent from the manifest — unaffected by Phases 0-6. |
| I-09 | `IdleService`'s default idle threshold (`300'000` ms) is an inline magic number, inconsistent with every sibling constant in the same unit | Confirmed | 81/100 (re-derived) | `IdleService.h` is absent from the manifest (only `IdleService.cpp` is, via the unrelated Phase 1 F-01 dedup — see I-03/I-05); `idle_threshold_ms_{300'000}` is still at line 79, unchanged. Independently re-verified all five sibling constants the original cited actually exist as named `static constexpr`: `kPollIntervalMs` (`SuspendInhibitorService.cpp:14`), `kTrackerThresholdMs` (`ExtIdleNotifyBackend.cpp:12`), `kWarningTimeoutMs`/`kCriticalTimeoutMs` (`LowBatteryMonitor.cpp:14-15`), `kMissingDaemonNotificationDelayMs` (`IdleService.cpp:26`) — confirming the "every other tunable duration" claim with per-site verification the original text asserted but didn't itemize. New, stronger sibling comparison beyond the original's list: `LowBatteryMonitor.cpp` additionally defines `kDefaultWarningThreshold{20}`/`kDefaultCriticalThreshold{10}` (lines 12-13) used as `QSettings` fallback defaults for a user-configurable value — the exact same "configurable-with-inline-fallback-default" shape as `idle_threshold_ms_` (which backs the WRITE-able `idleThresholdMs` property) — and even *that* closer analog uses named constants, foreclosing a "maybe it's different because it's configurable, not fixed" defense before it can be raised. Decomposition: pushed up by this more precisely-matched sibling comparison (a stronger version of the original's evidence, per the "sibling pattern comparison" category); held to 81, barely above the Confirmed floor, because this remains a purely cosmetic naming-convention issue with zero functional risk. Code-change note: `IdleService.h` not in the manifest — unaddressed. |
| I-10 | `SysfsBackend` ignores `QTextStream` numeric-extraction failure when reading sysfs values | Confirmed | 80/100 (re-derived) | `[Phase 1: 1e6ff14 — SysfsBackend.cpp's resolveSessionPath() was rewritten for the F-01 dedup, shifting readBrightness() from :134-143 to :101-110 and selectDevice()'s stream site from ~:63-74 to :63-64; neither extraction site's fail-state handling was touched — still applicable in new form (same gap, new line numbers)]`. Re-locating the cited code at current HEAD shows real line-number drift: `selectDevice()`'s `QTextStream(&max_file) >> max_val` is now at line 64 (minor ~4-line shift from the Phase-1 include trim), but `readBrightness()`'s `QTextStream(&file) >> val` shifted substantially, from the originally-cited `:134-143` down to current `:107-108` — a ~30-line shift caused entirely by Phase 1's unrelated `resolveSessionPath()` rewrite (the old ~36-line inline `GetSessionByPID`/loginctl block collapsed to a 2-line call to `resolveActiveLogindSessionPath()`), not by any change to the stream-reading logic itself. Neither extraction site gained a `.status()`/fail-state check in that refactor — the substantive gap is identical, just relocated. Decomposition: pushed up by confirming via the actual git diff (not assumption) that Phase 1 touched unrelated code in this file and definitively did not address this gap in passing; held to 80, the Confirmed floor, because the triggering scenario (readable-but-non-numeric sysfs content during a device-teardown race) remains narrow and low-frequency, matching the original's own Low severity/Effort S framing. |

**Verdict tally**: 7 Confirmed (I-01, I-03, I-04, I-06, I-07, I-09, I-10) · 3 Refuted (I-02, I-05, I-08) · 0 Not-a-bug · 0 Needs-Manual-Verification. No item required live-compositor verification — all 10 findings are D-Bus logic, signal ordering/lifecycle, or sysfs-parsing concerns fully resolvable by static re-read, consistent with this unit's backend-service nature (no QML/pointer/timing surfaces in scope).
