# U-10 Triage — Overlay Content & Shared QML Building Blocks

**Source**: docs/sdd/poc-readiness-review/units/U-10-overlay-controls-widgets.md (Investigation Targets section)
**Items in scope**: 7
**Methodology**: Re-read each cited file at current HEAD (locating the actual current line numbers rather than trusting the source doc's), grep-verified every claim about references/usages myself, cross-checked all cited files against `.manifest/phase0-6-changed-files.txt` and ran `git log -p <commit> -- <file>` on every match to see the exact Phase 0-6 diff before judging moot/partially-addressed/still-applicable — no automated tool substituted, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-001 | Icon `Image` elements outside the established fallback pattern have no error handling | Confirmed | 85/100 (re-derived) | [Phase 6: `LauncherRightPanelBrowse.qml` and `LauncherRightPanelSearch.qml` both gained `pragma ComponentBehavior: Bound` (line +1 shift) and `LauncherRightPanelSearch.qml` additionally got its `parent.modelData`→`actionDelegate.modelData` fix (D-004) — neither touches the icon `Image` blocks; still applicable in new form, lines shifted 64→65 and 174→175 respectively.] Re-verified: `LauncherRightPanelBrowse.qml:65-74` (recent-apps icon) and `LauncherRightPanelSearch.qml:175-184` (selected-entry icon) both still set only `source`/`fillMode`/`asynchronous`, no `onStatusChanged`/`Image.status` handling; `Tray/TrayMenuItem.qml:75-89` (not in manifest, unchanged) same gap. Directly contrasted against `LauncherResultRow.qml:53-73`, which I re-read and confirmed implements `icon.usedFallback` + `onStatusChanged: if (status === Image.Error && !icon.usedFallback) icon.usedFallback = true` — a working, in-repo reference implementation two directories away. Score decomposition: clear code citation on both sides of the contrast (+), purely static/structural finding requiring no runtime state to confirm (+), capped below 90 only because the practical impact (a blank icon slot) is a minor visual-polish gap, not a crash/data-loss defect. |
| I-002 | `TraySection.qml`'s per-delegate `_roles` re-reads all 12 model roles on every `TrayModel.revision` bump | Not-a-bug | — | Re-read `TraySection.qml:93-118` in full: the block carries its own explanatory comment (lines 94-99) stating this is "necessary because the Repeater's early-exit optimisation (QVariant deep-equal) prevents delegate recreation when only item data changes without slot-assignment changes" — i.e. the O(items×12) re-read is a deliberate, documented trade of role-read cost for correctness of delegate updates, not an oversight. File not in the Phase 0-6 manifest — unchanged since the original review, so the same deliberate design stands today verbatim. The suggested alternative (a per-row generation role) is a genuine optimization idea, but the current shape is not a defect: it is bounded by realistic StatusNotifierItem counts (single digits in practice), and the code explains its own reasoning inline rather than hiding an accidental N² pattern. |
| I-003 | `MouseArea` used for hover+click where sibling components already use `HoverHandler`+`TapHandler` | Not-a-bug | — | Re-read all three sites at current HEAD: `LauncherResultRow.qml:157-162` (unchanged, not in manifest) and `Tray/TrayMenuItem.qml:129-139` (unchanged, not in manifest) match the original citation exactly; `Launcher/LauncherActionRow.qml`'s `MouseArea{id:rowArea}` shifted 68→89 — [Phase 4: `LauncherActionRow.qml` gained a `selected` property plus two highlight `Rectangle`s (the D-001 keyboard-highlight fix) — unrelated to the hover/click MouseArea; still applicable in new form at the new line]. All three still use `MouseArea{hoverEnabled:true}` for simple hover+tap, and this remains functionally correct — no defect is alleged in the current behavior, only a stylistic inconsistency against `TrayItem.qml`. But `TrayItem.qml` itself (cited by the original finding as the "modern" counter-example) still keeps a plain `MouseArea` for its multi-button (`Qt.LeftButton\|RightButton\|MiddleButton`) dispatch — confirming the project's own convention is "pointer handlers where sufficient, MouseArea where structurally justified," not a blanket mandate. CLAUDE.md's own recorded gotcha documents that a syntactically-clean `WheelHandler`/`TapHandler` swap in this exact codebase "passed qml-lint, built clean, and passed the automated QML smoke test, but failed to receive wheel events at all in live testing" — i.e. the suggested "fix" itself carries known, undetectable-by-static-tools regression risk with no compensating functional benefit to the working MouseArea code. Keeping the proven pattern here is a reasonable, deliberate choice, not a bug. |
| I-004 | `StableDigitsText.qml`'s per-glyph delegate omits `textFormat: Text.PlainText` despite high update frequency | Confirmed | 84/100 (re-derived) | [Phase 6: file gained `pragma ComponentBehavior: Bound` as line 1 — unrelated to text rendering; still applicable in new form, delegate lines shifted 106-118 (was 105-117).] Re-read `StableDigitsText.qml:107-119`: the per-character `Text` delegate still sets only `text`/`color`/`horizontalAlignment`/`font.*`, no `textFormat`, so it retains the default `Text.AutoText` markup-detection scan on every glyph change. Confirmed consumed on a hot per-second path by `ClockWidget.qml`/`TimeToEventWidget.qml` (still true — grep shows both import `StableDigitsText` unchanged). Score decomposition: high certainty on the static code fact (exact default documented in Qt, directly observable, no runtime ambiguity) (+); capped in the mid-80s rather than higher because the magnitude of the saved cost (a markup scan on a 1-2 char string, once per second, across ~8 instances) is real but small — a "worth fixing, low urgency" profile rather than a severe defect. |
| I-005 | Stale width comment vs. actual `Layout.preferredWidth` on Launcher's right panel | Confirmed | 90/100 (re-derived) | [Phase 4: `Launcher.qml` gained `selected:` wiring on `actionRow` (~line 299, D-001 fix) — unrelated. Phase 6: `Launcher.qml` gained `pragma ComponentBehavior: Bound` plus the `section.delegate`'s `required property string section` fix (the CLAUDE.md `ListView.section.delegate` + Bound-mode gotcha) around lines 241-260 — unrelated. Neither touches the right-panel comment/width block; still applicable in new form, shifted 327-329→332-334.] Re-verified at `Launcher.qml:332-334`: the comment literally reads `// Right column — mode-switched panel (fixed 240px)` immediately above `Layout.preferredWidth: 256`. This is a trivial, unambiguous string-vs-value mismatch, fully resolvable by reading the two adjacent lines — no interpretation required. Score decomposition: maximal certainty on the fact itself (+), not pushed to the high-90s only because "worth fixing" here is about avoiding a *future* misread rather than a live defect today. |
| I-006 | Several `MultiEffect`/`Rectangle` ids declared but never referenced (dead ids) | Confirmed | 88/100 (re-derived) | [Phase 6: `Tray/TrayItem.qml`'s `visible` binding was changed from `root.status !== "Passive" && root.overflowVisible` to `root.opacity > 0 && root.overflowVisible` (the D-007 fade-out fix, +2 net lines from an added explanatory comment) — unrelated to any of the three flagged ids in this file; still applicable in new form, `frame`/`glowEffect`/`badgeGlow` shifted 77→80, 104→107, 144→147.] `Tray/TraySection.qml` (`frameCanvas`:63, `trayRepeater`:86, both unchanged, not in manifest) and `Notifications/ToastItem.qml` (`closeButton`:268, unchanged, not in manifest) match the original citation exactly. I independently grep-verified all 5 ids myself against the current file contents (not just trusted the original doc's "already checked" claim): `grep -n "frame\.\|glowEffect\.\|badgeGlow\."` in `TrayItem.qml`, `"frameCanvas\.\|trayRepeater\."` in `TraySection.qml`, and `"closeButton\."` in `ToastItem.qml` each return zero hits outside the declaration line itself. Since cross-file QML id references are illegal, a whole-file grep is conclusive. Score decomposition: maximal citation certainty (+), capped below 95 only because "harmless refactor debris vs. intentionally reserved for a future animation target" can't be fully ruled out by static reading alone — a judgment call the original review itself flagged as ambiguous. |
| I-007 | `WeatherIconCompositor.qml` carries undocumented properties vs. project memory | Confirmed | 87/100 (re-derived) | [Phase 6: `libs/holonight-services/src/weather-icon/WeatherIconBridge.cpp` changed — but only to extract `variantToMoonPhase()`'s hand-tuned thresholds (0.01/0.24/0.26/…) into named `constexpr` constants; the function's signature, behavior, and the QML-visible API surface are untouched. `WeatherIconCompositor.qml` itself is not in the manifest. Unrelated to the doc-drift finding; still applicable in new form, unchanged.] Re-read `WeatherIconCompositor.qml:9-15`: `conditionDescription`, `windSpeedKmh`, and `moonPhase` (`property var moonPhase: undefined`, used at line 21 as `var mp = root.moonPhase` and passed into `WeatherIconBridge.layersForWeather(...)`) are all still present and undocumented in the project memory file, which states "there is no separate `moonPhase` property; it's derived internally." I found stronger evidence than the original write-up: `WeatherIconBridge.h` declares **two** distinct invokables — `layersFor(condition_code, is_day, date)` (matches the memory doc exactly) and a separate `layersForWeather(condition_code, is_day, date, condition_description, wind_speed_kmh, moon_phase)` (the one this QML component actually calls) — so the drift is not just extra properties but an entirely different, newer entry point the memory file never mentions at all. Score decomposition: high certainty from direct three-way comparison of memory text, QML property list, and C++ header signatures (+); kept just under 90 because this is a documentation-maintenance issue rather than a code defect — "worth fixing" means updating the memory file, which is real but inherently lower-stakes than a runtime bug. |
