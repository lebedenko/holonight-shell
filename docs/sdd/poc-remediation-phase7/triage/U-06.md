# U-06 Triage — Launcher & Notifications Services

**Source**: docs/sdd/poc-readiness-review/units/U-06-launcher-notifications.md (Investigation Targets section)
**Items in scope**: 10
**Methodology**: Static re-read of all 10 cited locations against current HEAD (`LauncherService.{h,cpp}`, `LauncherModel.{h,cpp}`, `DesktopEntryCache.{h,cpp}`, `DesktopEntryScanner.{h,cpp}`, `DesktopEntrySerializer.h`, `NotificationRuleModel.{h,cpp}`, `NotificationService.h`), cross-checked line-by-line against `docs/sdd/poc-remediation-phase7/.manifest/phase0-6-changed-files.txt` and `git log -p` on every specific commit (both the 7 labeled `remediate phase N poc findings` commits and the 8 interspersed non-phase fixes) touching those files, plus a deliberate second pass hunting for Refuted/Not-a-bug candidates in the 3 most stylistic-looking items (I-01, I-06, I-08) per the SPEC.md self-check rule (see closing note); no automated tool substituted, matching the original review's methodology (REQ-NF-01).

## Verdict Table

| Item ID | Title | Verdict | Confidence | Rationale / Checklist |
|---|---|---|---|---|
| I-01 | `LauncherService::results()` is the sole non-const `Q_PROPERTY READ` accessor | Confirmed | 82/100 (re-derived) | [Phase 5 (3fd9628): `LauncherService.h` removed `rebuildDesktopFileIndex()`/`desktop_file_index_` (superseded by `LauncherModel`-owned index), an unrelated hunk that shifted this finding's citation by 1 line (`:57`→`:56`, `:85`→`:84`) — still applicable in new form.] Re-examined at current `LauncherService.h:56,84`: `results()` remains the only `Q_PROPERTY READ` accessor without `const` — every sibling (`query()`, `selectedIndex()`, `resultCount()`, `appResultCount()`, `actionResultCount()`, `activeCategory()`, etc.) is `const`. New evidence beyond the original write-up: a codebase-wide grep for `Q_PROPERTY(QAbstractItemModel*` shows a real, working precedent for the `const_cast` idiom — `NetworkService::wifiNetworks() const` returns `QAbstractItemModel*` as `const` today — proving this is fixable, not a QML/C++ constraint (one other sibling, `SuspendInhibitorService::inhibitorModelForQml()`, shares the same non-const pattern, so the codebase is internally inconsistent rather than uniformly one way). Decomposition: clear, single-line, unambiguous code citation (pushes score up) offset by genuinely cosmetic/low-severity impact with zero live call sites affected today (caps the score below 85). Code-change note: original finding cited `LauncherService.h:57,85`; unaffected in substance by any of Phases 0–6, only shifted by an unrelated member/method removal. |
| I-02 | Launcher rescan resets selection/scroll state unconditionally | Confirmed | 85/100 (re-derived) | [Phase 5 (3fd9628): `LauncherService.cpp`'s `connect(&model_, &QAbstractItemModel::modelReset, ...)` lambda renamed its callee from `rebuildDesktopFileIndex()` to `onModelEntriesReset()`; the reset-selection call (`refreshSelectionAfterModelReset()`) immediately following it is untouched — still applicable in new form.] Re-examined at current `LauncherService.cpp:192-197` (lambda) and `:578-582` (`refreshSelectionAfterModelReset()`): the lambda unconditionally calls `onModelEntriesReset()` then `refreshSelectionAfterModelReset()` on every `modelReset`, and the latter still does `const int clamped = resultCount() > 0 ? 0 : -1;` with no diffing against prior state — exactly as originally described. `LauncherModel::rebuildResults()` (current `:288-297`) still always does `beginResetModel()`/`endResetModel()` for both browse and search paths, with no entry-set-unchanged short-circuit. Decomposition: clear, deterministic dataflow citation, no timing ambiguity (this is provable from code alone — no compositor interaction needed to know the reset fires unconditionally), pushes score up; docked slightly because the *user-visible annoyance* (mid-navigation jump) depends on timing of background rescans, a minor subtlety. Code-change note: cosmetic rename only (Phase 5); the reset-unconditionally behavior is byte-identical to what the original finding described. |
| I-03 | `LauncherModel::data()` switch `default:` suppresses `-Wswitch` protection | Confirmed | 80/100 (re-derived) | [`LauncherModel.cpp` is in the baseline manifest (modified by Phases 4/5/6 — added `isActionRow`/`actionIndexAt`, `rebuildDesktopFileIndex`, static `roleNames()` caching) but none of those hunks touch the `data()` switch itself — the cited lines are byte-for-byte unchanged since the original review — still applicable in new form (file-level), unchanged at the cited lines.] Re-examined at current `LauncherModel.cpp:67` (`switch (static_cast<Role>(role)) {`) and `:120-121` (`default: return {};`) — identical line numbers to the original citation, confirming zero drift despite three phase commits touching this file elsewhere. All 15 `Role` enumerators are still explicitly cased, with the same trailing `default:`. Decomposition: trivial, 100%-certain static observation (pushes score up) but genuinely low real-world impact — the risk only materializes on a *future* `Role` addition and Qt's own `Qt::DisplayRole` etc. legitimately need a catch-all — caps the score at the Confirmed floor. Code-change note: no functional change since the original finding; only nearby (non-overlapping) code shifted. |
| I-04 | `LauncherModel` copies full `DesktopEntry` structs into `ScoredEntry` on every keystroke | Confirmed | 83/100 (re-derived) | [Phase 4 (2372ef6): `LauncherModel.cpp` inserted `isActionRow()`/`actionIndexAt()` (15 lines) immediately before `browseDisplayName()`, shifting `rebuildBrowseResults()`/`rebuildSearchResults()` from the original `:227-238,240-270` citation to current `:243-254,256-286` (+16 lines) — still applicable in new form.] Re-examined at current `LauncherModel.cpp:249` (`results_.append(ScoredEntry{.entry = entry, ...})` in `rebuildBrowseResults`) and `:260-272` (`rebuildSearchResults`, up to 1 app copy + 1 copy per matched action): `ScoredEntry::entry` (`LauncherModel.h:59`, was `:52-57`) is still a value-typed `DesktopEntry`, not an index/pointer, despite `entries_` being provably stable for the duration of a single `rebuildResults()` call (only `setEntries()` replaces `entries_` wholesale, and it always triggers its own fresh rebuild). Decomposition: clear structural citation, low ambiguity (COW refcount bump, not deep copy, tempers severity but not confidence-that-the-pattern-exists) — score reflects near-certainty this is real, moderated only by the already-acknowledged low actual cost. Code-change note: the copy pattern itself is untouched; only its line position moved due to unrelated method insertions. |
| I-05 | `NotificationRuleModel::rules_` has no upper bound | Confirmed | 87/100 (re-derived) | [b082681 "fix: resolve notification application metadata" + Phase 4 (2372ef6) + Phase 6 (cf76ba0): `NotificationRuleModel.cpp`/`.h` gained a `desktop_entries_` display-matching cache, `persist(action)` failure-reason plumbing (`rulePersistenceFailed` signal, addressing part of Confirmed Finding F-01), and a static-cached `roleNames()` — none of these three separate changes add any cap, LRU eviction, or count-based pruning to `rules_` — still applicable in new form.] Re-examined at current `NotificationRuleModel.cpp:78-125` (`ensureApp()` overloads) and `.h:69` (`QList<AppNotificationRule> rules_;`): `ensureApp()` still appends unconditionally for every unseen `app_name` and persists on every insert; `prunedRules()` (current `:165-177`) still only drops rules with a non-empty `desktop_entry` that no longer matches an installed app — rules from ad-hoc `notify-send` callers (empty `desktop_entry`) are still never pruned or capped. New evidence beyond the original write-up: this is the same class of unauthenticated-D-Bus-input growth vector as the already-Confirmed **F-02** (`Notify()` has no caller validation) — any process on the session bus can grow `rules_` (and its on-disk persistence) without bound simply by calling `Notify()` with novel `app_name` values, which elevates confidence this is a real, worth-fixing gap rather than a theoretical one. Decomposition: clear code citation + reinforcing cross-reference to an already-Confirmed sibling finding pushes the score to the higher end of the Confirmed range. |
| I-06 | `NotificationRuleModel`/`NotificationService` `Roles` enums lack an explicit underlying type | Confirmed | 81/100 (re-derived) | [Phase 4 (2372ef6)/b082681: `NotificationRuleModel.h` added `DisplayNameRole`/`DisplayIconRole` to the same unscoped `enum Roles` (now `:26-35`, was `:25`-ish before the two new enumerators); `NotificationService.h` itself is **not** in the baseline manifest — only `NotificationService.cpp`'s `roleNames()` body was touched (Phase 6/cf76ba0, unrelated static-cache change) — still applicable in new form for the `NotificationRuleModel.h` half, unaffected for `NotificationService.h`.] Re-examined at current `NotificationRuleModel.h:26-35` and `NotificationService.h:42-55`: both `enum Roles { ... }` blocks are still declared without `: int`, and both still carry the same NOLINT comment scoped only to `cppcoreguidelines-use-enum-class`/`performance-enum-size` (the unscoped-enum concern), not the missing-underlying-type concern. New evidence: `LauncherModel::Role` in the same review unit (`LauncherModel.h:15`, `enum class Role : std::uint16_t`) already proves an explicit underlying type coexists safely with `Q_ENUM`/QML role-int interop in this exact codebase — this forecloses a "maybe it's a QML constraint" Not-a-bug reading and supports Confirmed. Decomposition: clear, mechanical, near-zero-risk fix with a working in-repo precedent (pushes score up); capped near the floor because it's purely a style/consistency nit with no observed runtime effect. |
| I-07 | `DesktopEntryCache` issues one fresh prepared statement per desktop entry during validation | Confirmed | 82/100 (re-derived) | [Phase 6 (cf76ba0): `LauncherService.cpp` inserted `constexpr int kFileWatcherDebounceMs` above `persistValidationResult()`, shifting this citation from `:80-108` to current `:82-110` (+2 lines, unrelated to this finding); `DesktopEntryCache.cpp` was touched only by b204159's single-line `kCurrentVersion` value bump (`1`→`2`), which precedes `metadata()`/`upsert()` and causes zero line-count shift there — still applicable in new form.] Re-examined at current `LauncherService.cpp:82-110` (`persistValidationResult()` loop over `scan.entries`) and `DesktopEntryCache.cpp:135-152` (`metadata()`) / `:154-188` (`upsert()`): both still construct `QSqlQuery query(database)` fresh and call `.prepare(...)` inline on every invocation, with no hoisted/reused prepared statement across the loop. Confirmed still runs on the `QtConcurrent` background thread (not UI-blocking), matching the original's own severity capping. Decomposition: clear, exact-line-match citation for the DB half, minor documented drift for the caller half — both fully explained, no ambiguity, supports a solid Confirmed score without inflation. |
| I-08 | `DesktopEntryCache` has no self-healing path for an externally tampered schema | Confirmed | 81/100 (re-derived) | [b204159 "fix: improve sidebar notification rule visibility": `DesktopEntryCache.cpp` bumped `kCurrentVersion` from `1` to `2` for the new `startup_wm_class` column — a legitimate, code-driven schema evolution — but `open()`'s version-pragma-only tamper-detection logic itself (current `:48-90`) is untouched by that commit or any other Phase 0–6 commit — still applicable in new form.] Re-examined at current `DesktopEntryCache.cpp:48-90`: schema validity is still driven entirely by comparing `PRAGMA user_version` against `kCurrentVersion`; `CREATE TABLE IF NOT EXISTS` remains a no-op once the version matches, with no `PRAGMA table_info` cross-check against the expected column set, and query failures (`loadAll`/`metadata`/`upsert`, all still only `qCWarning`) have no automatic drop-and-rebuild fallback. New evidence beyond the original write-up: the `kCurrentVersion` bump that happened for real between the original review and now (v1→v2, for a genuinely new column) proves the version-pragma mechanism is actively exercised by this codebase's own evolution — yet even this real schema change didn't add any external-tamper self-heal, since the code path for "your own migration" and "someone else broke the file" were never distinguished by design. Decomposition: clear citation, low likelihood of the specific tamper scenario in practice (single-user cache DB), which tempers the score without dropping it below the Confirmed floor — this is a real, if low-priority, robustness gap, not a false positive. |
| I-09 | `DesktopEntry` field list is independently enumerated in ≥3 places | Confirmed | 85/100 (re-derived) | [b204159 + 7b861a4: `DesktopEntryScanner.cpp`/`.h` and `DesktopEntrySerializer.h` all gained the new `startup_wm_class` field (parser `applyDesktopEntryField()`, both `toJson`/`fromJson` directions); `LauncherModel.cpp`'s `data()` role switch (current `:67-122`) was correctly **not** given a matching role since `startup_wm_class` has no QML consumer — still applicable in new form, now with a concrete post-review instance of the exact risk.] Re-examined at current `DesktopEntryScanner.h:12-26` (13-field `DesktopEntry` struct, up from the original review's count), `DesktopEntryScanner.cpp` parser, `DesktopEntrySerializer.h` (`toJson`/`fromJson`), and `LauncherModel.cpp:67-122`: all sites remain in sync for existing/used fields — no active bug — matching the original characterization. New evidence: the `startup_wm_class` addition since the original review is a live example of the exact shotgun-surgery risk described — it required a human to correctly touch 2 of the 3 (now arguably 4, counting `NotificationRuleModel::desktopEntryMatchesAppName()`, added in b082681, which independently reads `entry.startup_wm_class`) enumeration sites and correctly *not* touch the third, with zero compiler enforcement and still no code comment on the `DesktopEntry` struct pointing readers at the sibling sites. Decomposition: clear citation plus a fresh, concrete confirming instance (not hypothetical) pushes the score above the original 63/100 write-up; capped below 90 because "no active bug currently" remains true and the risk is forward-looking. |
| I-10 | `DesktopEntryCache` destructor doesn't release its `QSqlDatabase` connection | Confirmed | 84/100 (re-derived) | [`DesktopEntryCache.h` is untouched by any Phase 0–6 commit — absent from the baseline manifest entirely; `DesktopEntryCache.cpp` was touched only by b204159's unrelated single-line `kCurrentVersion` bump, nowhere near `close()`/the destructor — still applicable in new form (noting the file itself never changed in substance).] Re-examined at current `DesktopEntryCache.h:17-18` (`DesktopEntryCache() = default; ~DesktopEntryCache() = default;`) and `.cpp:92-98` (`close()`): the destructor is still `= default` and never calls `close()`; `open()` still registers a uniquely-named connection (`QUuid`-suffixed) via `QSqlDatabase::addDatabase`, and nothing in the class enforces the open/close pairing beyond caller discipline. All current call sites (`LauncherService.cpp` — `load_cache`, `rebuild_cache_writer`, and the free-function `validateAgainstCache()`'s local `cache`) do correctly pair `open()`/`close()` manually today, matching the original's own "not currently exploited" framing. Decomposition: clear, simple, zero-ambiguity RAII-violation citation (pushes score up); the only reason this isn't higher is that it requires a future early-return/exception/refactor to actually leak, which is speculative rather than demonstrated. |

---

**Note on the zero Refuted/Not-a-bug outcome**: per SPEC.md's guardrail, finding zero Refuted/Not-a-bug items across this unit's 10 targets was treated as a signal to re-check rigor rather than a badge of thoroughness. The three items most likely to resolve to Not-a-bug or Refuted on a shallow pass were deliberately re-examined harder: **I-01** (is a non-const model accessor actually required by QML/Qt?) — refuted as a design necessity by finding `NetworkService::wifiNetworks() const` as a working in-repo counter-example, which is why I-01 stayed Confirmed rather than flipping to Not-a-bug. **I-06** (does an explicit enum underlying type actually work with `Q_ENUM`/QML role interop here?) — the same codebase already proves it does (`LauncherModel::Role : std::uint16_t`), again supporting Confirmed over Not-a-bug. **I-08** (is the schema-tamper gap an accepted Stage-1 limitation like the sibling F-03 mtime/size gap?) — checked `docs/sdd/launcher-desktop-cache/SPEC.md` and `DESIGN.md` directly; neither document scopes out schema-tamper resilience the way SPEC.md explicitly scopes out sub-second mtime precision for F-03, so there is no citable architectural rationale to support Not-a-bug for I-08. All three searches came back supporting Confirmed with concrete evidence rather than a rubber-stamped default, which is the intended outcome of the guardrail check, not a failure of it.
