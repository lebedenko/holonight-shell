# UQC-211 — Batch 6 shell stability and composition

## Assignment and state

Ready → In Progress on 2026-09-15, before implementation. Canonical origin/main
was checked with `git ls-remote`; all checkouts were clean at assignment.

- Umbrella checkpoint: `eb9b80c`.
- Shell baseline: `fffb1715bac5a57127af5e671033ac33335ffc16`.
- Provider baseline: `638eec25c0934538e747b969b37ab8f63d1722de`.
- AI evidence baseline: `7e25e78cc7fb33aa63ed480bc3f328a65e04ca0c`.

## Scope and ownership

Investigate S01 fractional-scale instability and S02 topbar frame/status
composition. Repair only demonstrated shell causes, with a failing deterministic
regression first. Provider configure/hosting defects require a separate provider
package; do not compensate for them in shell composition.

F05 remains an umbrella investigation using the logged-out, disabled-provider AI
settings form. Record the exact control, session/compositor keyboard ownership,
Qt activation and active focus item, and Tab/Backtab delivery/acceptance around a
user-operated VT round trip. Observe navigation keys and focus metadata only.
AI/provider repairs require separate work packages. Never force focus or automate
pointer/focus interaction.

## Reproduction and geometry contract

Keep compositor output scale 1, output dimensions, profile and service state fixed.
Compare isolated Hyprland with HoloNight/Fusion and Qt scale 1/1.25. Record actual
DPR rather than inferring it from the environment. Observe startup first, then
user-operated hover and popup sequences. Compare scale-1 Sway for S02.

Trace compositor configure dimensions, Qt window/root dimensions, section scene
bounds, clipping, buffer dimensions, input regions and exclusive zone together.
Protocol surface coordinates, Qt device-independent coordinates, buffer pixels
and item transforms must be distinguished. The shell requests top/left/right
anchors, width 0, height 64 and exclusive zone 64. The provider owns mapping these
requests/configures to the Qt window. Hover transforms must not repeatedly resize
the layer surface. Sections must remain inside the usable bar after settling;
service-driven appearance/disappearance remains supported.

For S02 compare isolated frames with assembled sections at scale 1. Preserve the
intentional -12 primary-section overlap unless measured overpainting demonstrates
a defect. Network/audio/battery/layout normal fills are transparent; preserve
hover/active feedback. Notification/date sections are visual references.

## Verification and acceptance

Run focused geometry/rendering checks before the shell suite, formatting, QML
lint/types, import policy, architecture and licensing checks applicable to edits.
Verify installed behavior under both styles/scales. Publish verified shell commits
and confirm canonical availability before the coordinator pins them.

The umbrella owns the immutable Batch 6 kit, process outcome/hash/restoration
verification, manual instructions, and canonical results in FINDINGS.md. UQC-211
stays In Progress until S01/S02 acceptance or an explicit evidence-based
disposition. F05 cannot be closed by shell acceptance. Keep UQC-201 In Progress
and the initiative Accepted. Batch 4 acceptance, P01 external disposition,
removed Batch 5, reviewed Batch 7 and deferred Batch 3 findings are preserved.

## Investigation log

- Initial source trace: `LayerShellManager::configureSurface` requests fixed
  height/exclusive zone; `PerMonitorLayerManager` delegates sizing to provider
  `LayerSurfaceHost`. Provider `applyConfigureSize` currently copies protocol
  width/height directly into Qt window dimensions. Runtime evidence is required
  before attributing the historical jitter to that path.
- Initial composition inspection: status widgets use transparent resting fills;
  primary section overlap is explicit in TopBar. No speculative repair made.


## Checkpoint implementation

No shell product repair is justified by the evidence collected so far. Added
`tests/qml/tst_TopbarComposition.qml` with four compiled style/scale CTest entries;
the fixtures verify settled section bounds, resting status/frame pixel equality,
complete right edges, and active feedback returning to transparency. Completed
missing status-popup/tray-menu metadata in `FakeQmlServices.h` so those tests load
without undefined-property diagnostics. The harness reuses its existing image
wrapper for DPR-aware window crops: QtTest `grabImage()` truncated the initial
fractional-DPR fixture. No product change follows from that test defect.

The provider-only reduced experiment demonstrates a configure/Qt coordinate
mismatch without shell composition. Proposed provider package UQC-213 stays
Planned; stop the provider repair branch here. Historical Hyprland jitter and
real-compositor S02 remain unconfirmed. See the umbrella canonical findings for
raw evidence, exact measurements, shutdown limitations and manual handoff.

Verification: focused compiled HoloNight/Fusion × DPR 1/1.25 composition checks;
full underlying CMake build and isolated CTest suite; formatting, QML lint/types,
import-policy, architecture and REUSE licensing. `task test` and task formatting
stop at their existing hardcoded provider `68b7069` guard, so the individual
provider-build task and underlying build/check commands use the requested
`638eec2` checkpoint without changing dependencies. The parallel private-bus suite
passed 1164 entries, failed six and skipped one due to shared fake-service names;
all nine NetworkManager/portal follow-up entries pass serially. New compiled
composition entries are verified separately after registration. Existing QML
lint warnings remain; no warnings are suppressed by these changes.

Manual HoloNight/Fusion Hyprland runs at scales 1/1.25, scale-1 Sway frame checks,
and umbrella F05 VT comparisons remain pending. UQC-211 remains In Progress.
